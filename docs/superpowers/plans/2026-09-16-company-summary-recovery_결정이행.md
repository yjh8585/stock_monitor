# 결정 이행 기록 — 2026-09-16 회사 설명·제품 데이터 정본 복구

계획서 = [`2026-09-16-company-summary-recovery.md`](./2026-09-16-company-summary-recovery.md) 「확정된 결정」

원칙: **동작 불가**(계획서가 적은 컬럼이 없다·형식이 다르다)는 고치고 여기 기록만 남긴다.
**구조 선택**(어디서 돌릴지·어느 값을 정본으로 볼지·규칙 범위)은 🔴 **코드를 쓰기 전에** AskUserQuestion 으로 묻는다.

## 이 계획서의 결정

| # | 결정 (계획서 문구) | 이행 | 근거 |
| --- | --- | --- | --- |
| 1 | 국내 상장사 180곳 전부 fnguide 정본으로 되돌린다 | **그대로 · 완료** | 2026-09-16 실행: OK 180건 · 미수집 0 · 폴백 0 · 로드 실패 0. DB 확인 180/180 「동사」 시작, 평균 450자 |
| 2 | 덮어쓰기 가드 + 검사기 신설. 수집 주기 단축은 하지 않는다 | **그대로** | Task 2·4 로 구현. `collect-financials.yml` 의 분기 1회 cron 은 손대지 않는다 |
| 3 | 빈 칸일 때만 LLM 허용 | **그대로** | `lib/companies.py` 의 `keeps_existing_summary()` 가 「정본 회사 **and** 설명이 이미 있음」일 때만 True |
| 4 | 휴머노이드 비상장 9곳 전체 재조사 후 정정 | **그대로 · 조사 완료** | 퍼플렉시티 + 공식 홈페이지 직접 확인으로 9곳 조사 완료(퓨트로닉 1 + 4 + 4). 적재는 Task 6 |
| 5 | 대표 제품군 5~8개로 묶는다 | **그대로** | Task 6 Step 1 |
| 6 | 한화로보틱스는 휴머노이드 페이지에 남기되 설명을 사실대로 | **그대로** | Task 6 Step 3 |
| 7 | 시가총액은 GHA 에서 pykrx 원인을 먼저 판다 | **그대로** | Task 7 Step 2. 네이버 API 는 정상 응답을 확인만 해 두고 대안으로 보류 |
| 8 | 계획서를 새로 만든다 | **그대로 · 완료** | AskUserQuestion 승인 2026-09-16 |

## 상위 계획서(`agents/.../2026-08-24-humanoid-research.md`)의 결정 중 이번 작업에 걸리는 것

| # | 결정 | 이행 | 근거 |
| --- | --- | --- | --- |
| 6 | 부품 카테고리 세분 11종이 정본 | **그대로** | 퓨트로닉 내재화 모터에 `모터` 카테고리를 쓴다. 목록은 코드에 박지 않고 `product_categories.py` 에서 읽는다 |
| 7 | 비상장사는 재무 또는 기업가치가 있는 곳까지 | **그대로** | 대상 9곳을 늘리거나 줄이지 않는다 |
| 12 | 고객사는 컬럼 삭제·수집하지 않는다 | **그대로** | 🔴 제품을 정정하면서 고객사를 함께 채우지 않는다. 조사 보고에 고객사가 실려 있어도 DB 에 넣지 않는다 |

## 실측이 계획서와 달랐던 것 (동작 불가 → 고치고 기록)

| 언제 | 계획서 전제 | 실측 | 처리 |
| --- | --- | --- | --- |
| 2026-09-16 | 시가총액 값이 **틀렸을** 수 있다(현대차 127.8조) | 계산이 틀린 게 아니라 **낡았다** — 실제 75.1조(네이버)이고 52주 최고가 787,000원이라 과거 62만원대 시절 값이 멈춘 것 | 계획서 「조사로 확정된 사실」과 Task 7 Step 4 를 실측값으로 고쳐 적었다. 결정은 안 바뀐다 |
| 2026-09-16 | 엔알티 설명이 의심스럽다(정수기 부품 + 엔코더 조합) | **사실이었다.** 공식 홈페이지에 네 개 제품 카테고리가 나란히 있고 엔코더는 `NRT Sensors` 브랜드 | Task 6 Step 3 에 「고치지 말 것」으로 명시 |
| 2026-09-16 | 복구 전에 Task 3(revalidate 수정)을 끝낸다 | 조사·계획과 병행하느라 **복구를 먼저 돌렸다** | 캐시가 자동 무효화되지 않았으므로 Task 5 Step 3 에서 화면을 확인하고, 필요하면 `revalidate_tags(['companies'])` 를 수동 호출한다 |

## 변경 요청한 결정

없음(2026-09-16 기준).
