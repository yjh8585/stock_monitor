# 코드 리뷰 결과 — 관련회사/국내자동차 Tier 1 (Codex)

- 작성일: 2026-05-08
- 리뷰어: Codex (gpt-5.5)
- 리뷰 범위: `review/SCOPE.md` Tier 1 중심, Tier 2 migration 일부 확인

## 1. 요약

- 총 발견: **P0 0개 / P1 5개 / P2 8개**
- 핵심 risk Top 3:
  1. Supabase 응답을 수동 타입 단언으로 처리해 DB/view 변경 시 런타임 오류가 조용히 유입될 수 있음.
  2. 동적 Supabase 페이지에 route-level `loading.tsx`/`error.tsx`가 없어 Next.js 16 스트리밍/에러 경계가 약함.
  3. 테이블/행 상호작용이 마우스 중심이라 키보드 접근성과 ARIA 상태 전달이 부족함.

## 2. 카테고리별 상세

### A. 타입 안정성 ❌

| 우선순위 | 위치 | 지적 | 권고 |
|---|---|---|---|
| P1 | `app/related-stocks/page.tsx:23`, `app/domestic/page.tsx:22`, `components/related-stocks/NewsModal.tsx:77` | Supabase 결과를 `as RelatedStockRow[]`, `as DomesticStockRow[]`, `as NewsItem[]`로 단언 | Supabase generated `Database` 타입 또는 Zod DTO parse 도입 |
| P1 | `lib/types.ts:41`, `lib/types.ts:86` | view DTO가 수동 정의라 migration/view와 타입이 분리됨 | `Database['public']['Views']...` 기반 타입 생성 후 View DTO로 좁히기 |
| P2 | `lib/supabase/server.ts:9`, `lib/supabase/server.ts:10` | env 비-null 단언 | 서버 시작 시 명시 검증 또는 config 모듈에서 fail-fast 처리 |

### B. 컴포넌트 분리·재사용 ⚠️

| 우선순위 | 위치 | 지적 | 권고 |
|---|---|---|---|
| P2 | `components/related-stocks/StockRow.tsx:48`, `components/domestic/DomesticRow.tsx:65`, `components/common/StickyTable.tsx:63`, `components/related-stocks/NewsModal.tsx:57` | 30줄 기준을 크게 넘는 컴포넌트/로직 | 링크 버튼, row state, financial cells, news list 등을 하위 컴포넌트/hook으로 분리 |
| P2 | `components/related-stocks/StockTable.tsx:117`, `components/domestic/DomesticTable.tsx:112` | 정렬/필터/컬럼 생성 로직이 유사하게 중복 | 공통 table hook 또는 column factory 검토 |

### C. Next.js 16 데이터 페칭·렌더링 ⚠️

| 우선순위 | 위치 | 지적 | 권고 |
|---|---|---|---|
| P1 | `app/related-stocks/page.tsx:6`, `app/domestic/page.tsx:6` | `force-dynamic` Supabase 페이지인데 `loading.tsx`/`error.tsx` 없음 | 각 route에 `loading.tsx`, client `error.tsx` 추가 |
| P2 | `lib/supabase/server.ts:1` | server-only 모듈이 `server-only`로 보호되지 않음 | `import 'server-only'` 추가 검토 |

### D. 상태 관리 ✅

로컬 UI 상태 중심이며 서버 상태를 Zustand에 중복 저장하지 않아 현재 범위에서는 큰 문제 없음.

### E. 폼·유효성 ⚠️

| 우선순위 | 위치 | 지적 | 권고 |
|---|---|---|---|
| P1 | `app/api/news/search/route.ts:69`-`75` | URL query 입력을 Zod/길이 제한 없이 외부 검색 요청에 사용 | `q`, `country` schema 검증, 최대 길이, 허용 country enum 적용 |

### F. 접근성 ❌

| 우선순위 | 위치 | 지적 | 권고 |
|---|---|---|---|
| P1 | `components/common/StickyTable.tsx:165`, `components/common/StickyTable.tsx:177` | `<th>` 클릭 정렬/리사이즈가 키보드 조작 불가 | 내부에 `<button>` 사용, `aria-sort`, 키보드 리사이즈 대안 제공 |
| P1 | `components/related-stocks/StockRow.tsx:101`, `components/domestic/DomesticRow.tsx:111` | 클릭 가능한 `<tr>`가 keyboard/focus 대상 아님 | 행 선택 버튼 또는 `tabIndex`/keyboard handler/상태 aria 추가 |
| P2 | `components/related-stocks/ProductCell.tsx:20`, `components/related-stocks/NewsModal.tsx:100`, `components/common/ToggleFilterBar.tsx:81` | 아이콘 버튼에 `aria-label` 부족 | title 의존 대신 명시적 `aria-label` 추가 |

### G. 성능 ⚠️

| 우선순위 | 위치 | 지적 | 권고 |
|---|---|---|---|
| P2 | `app/related-stocks/page.tsx:14`, `app/domestic/page.tsx:14` | view `select('*')` 사용 | 화면에 필요한 컬럼 명시 |
| P2 | `components/related-stocks/CustomerBadges.tsx:20`-`23` | 외부 SVG를 raw `<img>`로 로드하고 lint 우회 | 로고 정책을 정리하고 `next/image` 또는 로컬 캐시/허용 도메인 검토 |

### H. 에러 핸들링·로깅 ⚠️

| 우선순위 | 위치 | 지적 | 권고 |
|---|---|---|---|
| P2 | `components/related-stocks/NewsModal.tsx:71`-`95`, `app/api/news/search/route.ts:77` | Supabase/API 오류가 사용자 메시지·로그 없이 빈 목록으로 흡수됨 | Pino 서버 로그, client error state/toast, `finally setLoading(false)` 적용 |
| P2 | `app/related-stocks/page.tsx:20`, `app/domestic/page.tsx:19` | view 조회 실패 후 빈 테이블로 계속 렌더 | 치명/부분 실패 기준 분리, route error boundary로 throw 여부 결정 |

### I. 보안 ⚠️

| 우선순위 | 위치 | 지적 | 권고 |
|---|---|---|---|
| P1 | `supabase/migrations/20260507000006_add_domestic_page_support.sql:40`, `:59`, `:126` | `company_pages` anon read 전체 허용 및 view가 RLS 의도를 명시하지 않음 | Supabase view `security_invoker` 및 anon 공개 범위 재검토 |
| P2 | `app/api/news/search/route.ts:33`-`38` | 외부 뉴스 fetch를 무제한 입력으로 호출 가능 | rate limit/cache/입력 길이 제한 추가 |

### J. DB·migration·트랜잭션 ⚠️

| 우선순위 | 위치 | 지적 | 권고 |
|---|---|---|---|
| P2 | `supabase/migrations/20260507000006_add_domestic_page_support.sql:10`-`126` | 컬럼 변경, 테이블 생성, 정책 생성, view 재생성이 한 migration에 있으나 명시 transaction 없음 | 실패 시 중간 상태 방지를 위해 transaction 또는 단계 분리 |
| P2 | `supabase/migrations/20260507000006_add_domestic_page_support.sql:57`, `:59`, `:126` | view drop/create 반복으로 의존 객체 영향 검증 어려움 | 의존 view/권한/보안 옵션을 migration별 명시 |

### K. 반응형 & UI 일관성 ⚠️

| 우선순위 | 위치 | 지적 | 권고 |
|---|---|---|---|
| P2 | `components/common/ToggleFilterBar.tsx:63`, `components/common/ToggleFilterBar.tsx:91`, `components/domestic/DomesticFilterBar.tsx:128` | 필터바가 `flex-wrap`만 의존하고 우측 단위 라벨은 `ml-auto shrink-0`라 좁은 화면 overflow 가능 | 모바일에서 라벨 줄바꿈/숨김/별도 row 처리 |

### L. 프로젝트 컨벤션 준수 ⚠️

| 우선순위 | 위치 | 지적 | 권고 |
|---|---|---|---|
| P2 | `components/common/StickyTable.tsx:31`, `components/related-stocks/StockTable.tsx:20`, `components/domestic/DomesticTable.tsx:18` | 연도/폭/팝업 크기 등 매직 넘버가 산재 | 의미 있는 상수로 분리 |
| P2 | `components/related-stocks/StockRow.tsx:48`, `components/domestic/DomesticRow.tsx:65` | 함수 30줄 이하 컨벤션 미준수 | 표시 cell 단위 분리 |

## 3. 우선 조치 권고 Top 5

1. **P1** — `app/api/news/search/route.ts:69` query param Zod 검증과 길이 제한 추가.
2. **P1** — `app/related-stocks/page.tsx:23`, `app/domestic/page.tsx:22` Supabase 응답 타입 단언 제거.
3. **P1** — `app/related-stocks`, `app/domestic`에 `loading.tsx`/`error.tsx` 추가.
4. **P1** — `components/common/StickyTable.tsx:165` 정렬 헤더를 키보드 접근 가능한 버튼 구조로 변경.
5. **P1** — `supabase/migrations/20260507000006_add_domestic_page_support.sql:59` view/RLS 공개 범위와 `security_invoker` 정책 재검토.

## 4. 카테고리 외 발견 사항

- `any` 직접 사용과 `console.log`는 Tier 1 범위에서 발견하지 못했습니다.
- 이번 세션은 read-only라 파일 생성/수정 및 타입체크 산출물 갱신은 수행하지 않았습니다.