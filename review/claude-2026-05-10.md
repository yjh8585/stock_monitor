# 코드 리뷰 결과 — 2026-05-10 누적 (Claude)

- **작성일**: 2026-05-10
- **리뷰어**: Claude Opus 4.7
- **리뷰 범위**: `review/SCOPE-2026-05-10.md` (5월 8일 SCOPE 잔존 검증 + 신규 OEM/parts-top100 + domestic 정정 마이그레이션)
- **체크리스트**: `review/CHECKLIST.md` (12 카테고리 A~L)

---

## 1. 요약

- **총 발견**: **P0 6개 / P1 18개 / P2 14개**
- **5월 8일 P0/P1 잔존**: P0 4개 모두 잔존, P1 26개 중 약 20개 잔존 (대부분 미수정)
- **신규 변경에서 발견**: P0 2개, P1 8개

### 핵심 risk Top 3

1. **OEM 5개 신규 테이블 RLS 정책 누락 (`I`/`J` P0 + 5월 8일 I-1 잔존)** — anon key가 client에 노출된 환경에서 새 마이그레이션 `20260510000001_create_oem_sales.sql`은 RLS enable 한 줄도 없음. parts_top100_stocks_view + 5월 8일 미해결까지 합치면 외부 무방비 영역이 더 넓어짐.
2. **/oem 페이지 첫 응답 ~9초 / 17개 테이블 fetch + 117K행 client 직렬화 (`G` P0)** — `app/oem/page.tsx:43-48`이 4개 테이블을 페이지네이션 fetch 후 `groupCountryMonth` 117K 행을 그대로 OemDashboard prop으로 직렬화. RSC payload 기가바이트급 위험 + 클라이언트 무거운 집계.
3. **5월 8일 Table/Row/formatRateLabel 공통화 P0 모두 잔존 + parts-top100이 DomesticTable을 부분 재사용했지만 StockTable과는 여전히 중복 (`B`)** — 600줄+ 중복 코드가 그대로 + 신규 페이지 추가 시 또 복사 필연. OEM은 신규 13 차트 컴포넌트 별도 디렉토리로 분리됐지만 helpers/색상 팔레트 일관성은 OK.

---

## 2. 카테고리별 상세

### A. 타입 안정성 ⚠️

| 우선순위 | 위치 | 지적 | 권고 |
|---|---|---|---|
| P1 | `app/oem/page.tsx:33` | `(data as T[])` 단언 — fetchAll generic이 외부에서 타입 강제, 런타임 검증 없음 | Zod parse 또는 supabase generated 타입 + view 매핑 |
| P1 | `app/oem/page.tsx:43-48`, `app/parts-top100/page.tsx`, `app/related-stocks/page.tsx:23`, `app/domestic/page.tsx:22` | 5월 8일 A-1 **잔존** + 신규 페이지에도 동일 패턴 | 일괄 적용 (헬퍼 `safeFromView<T>(supabase, table, schema)`) |
| P2 | `lib/types.ts:155` (OEM PowerTrain) | `powertrain: PowerTrain \| string` — string union이 PowerTrain enum의 타입 안정성 무력화 | string 빼고 `PowerTrain`만, DB 정규화 보장 (스크립트로) |
| P2 | `components/oem/helpers.ts:117` | `r.powertrain` (string)을 키로 Map에 넣고 차트에서 `PT_ORDER` (PowerTrain[])로 조회 — 미정규화 값 들어오면 차트 빈 영역 | normalize 1차 검증 + 'Other' fallback 명시 |
| P2 | `app/oem/page.tsx:18` | `Awaited<ReturnType<typeof createSupabaseServerClient>>` 인라인 타입 | `lib/supabase/server.ts`에서 `SupabaseServerClient` 타입 export |
| P2 | 5월 8일 A-2/3/4 잔존 | `lib/supabase/server.ts:9-10` env 비-null 단언, `r.base as keyof ExchangeRates` 단언, `key: 'name_kr'` 컬럼 키 중복 | 5월 8일 권고와 동일 |

### B. 컴포넌트 분리·재사용 ❌

| 우선순위 | 위치 | 지적 | 권고 |
|---|---|---|---|
| **P0** | `components/related-stocks/StockTable.tsx` ↔ `components/domestic/DomesticTable.tsx` | 5월 8일 B-1 **완전 잔존** (~400줄 중복) | `BaseStockTable<R, K>` + `useStockTableState<R, K>` hook 추출 |
| **P0** | `components/related-stocks/StockRow.tsx` ↔ `components/domestic/DomesticRow.tsx` | 5월 8일 B-2 **완전 잔존** (~150줄 중복) | 셀 헬퍼 분해 |
| **P0** | 5월 8일 B-3 | `formatRateLabel` 중복 — **잔존** | `lib/exchangeRates.ts` 추출 |
| P1 | `components/oem/Top30YtdChart.tsx:25-109`, `Top10AnnualBars.tsx`, `Top10MonthlyLines.tsx`, 등 | 차트 컴포넌트 모두 80~110줄 (30줄 컨벤션 위반) — 차트 데이터 가공 로직 + 차트 마크업이 한 함수 | 데이터 변환을 `helpers.ts`에 함수로 분리, 차트 컴포넌트는 마크업 + props만 |
| P1 | `components/oem/Top30YtdChart.tsx:46-107` | 좌(차트) + 우(표) 듀얼 레이아웃이 `EvLeadersChart`, `YoyWinnersLosers`에서 반복 | `<DualPanel left right />` 헬퍼 컴포넌트 |
| P1 | `components/oem/Top30YtdChart.tsx`, `CountryTop15.tsx`, `PowertrainTopOems.tsx`, `EvLeadersChart.tsx` | 가로 막대 차트 마크업 (BarChart layout="vertical" + XAxis/YAxis/Cell map) 중복 4번 | `<HorizontalBarChart data={...} colorFn={...} />` 추출 |
| P2 | `components/oem/OemDashboard.tsx:33-95` | Section wrapping + title/subtitle + content가 13번 반복 | `SECTIONS` 배열로 데이터화하고 map 렌더 |
| P2 | 5월 8일 B-4/5/6 잔존 | `openCompanyLink`, `ToggleFilterBar` 미활용, 토글 핸들러 중복 — 모두 미수정 | 5월 8일 권고와 동일 |

### C. Next.js 16 데이터 페칭·렌더링 ⚠️

| 우선순위 | 위치 | 지적 | 권고 |
|---|---|---|---|
| P1 | `app/oem/`, `app/parts-top100/`, `app/related-stocks/`, `app/domestic/` | 5월 8일 C-1 **잔존** + OEM/parts-top100도 동일 — `error.tsx`/`loading.tsx` 모두 누락 | 4개 디렉토리에 통일 추가 |
| P1 | `app/oem/page.tsx:11`, `app/parts-top100/page.tsx`, 기존 페이지 | 5월 8일 C-2 **잔존** + OEM/parts-top100도 `force-dynamic` — Cache Components 미활용 | `'use cache'` + `cacheTag('oem_sales')` + `import_oem_sales.py`가 적재 후 `updateTag` |
| P1 | `app/oem/page.tsx:28-31` | fetchAll: error 시 `return all` (부분 결과로 진행) — **차트가 잘못된 데이터로 렌더되어 사용자가 인지 못 함** (이번 세션 PostgREST max-rows 버그가 정확히 이 경로로 잠복했었음) | error throw + error.tsx로 라우트 fallback |
| P1 | `app/oem/page.tsx`, `app/parts-top100/page.tsx`, `app/related-stocks/page.tsx`, `app/domestic/page.tsx` | 5월 8일 H-1 잔존 + 신규도 viewErr 시 logger만 | error throw |
| P1 | `components/oem/OemDashboard.tsx:1` | `'use client'` + 13개 차트 모두 client → 117K 행 RSC 직렬화 → 클라이언트에서 다시 useMemo 집계 | 서버 컴포넌트 page.tsx에서 미리 사전 가공한 작은 props만 client에 전달 (예: TOP10 미리 추출, Country×OEM 매트릭스 미리 계산) |
| P2 | OEM 차트 11개가 recharts 사용 | recharts 번들 (~200KB) 첫 페이지에 동봉 | `dynamic(() => import('./Top30YtdChart'), { ssr: false })` 적용 (5월 8일 G-2 권고와 동일) |
| P2 | 5월 8일 G-1, C-5 잔존 | exchange_rates_live 캐시, view `select('*')` | 5월 8일 권고와 동일 |

### D. 상태 관리 ✅

OEM 컴포넌트들도 모두 props + useMemo + useState(탭 active만). Zustand 도입 불필요. 현재 범위에서 OK.

### E. 폼·유효성 ✅

OEM/parts-top100에 폼 없음 — 5월 8일과 동일.

### F. 접근성 (a11y) ❌

| 우선순위 | 위치 | 지적 | 권고 |
|---|---|---|---|
| P1 | `components/oem/PowertrainTopOems.tsx:46-71` | 탭 버튼 4개에 `aria-pressed`, `role="tab"`, 키보드 좌우 화살표 네비게이션 없음 | shadcn Tabs 사용 (이미 `components/ui/tabs.tsx` 존재) |
| P1 | `components/oem/OemCountryHeatmap.tsx:55-104` | 히트맵 셀이 `<td>` + `title` 만 — 시각장애인은 매트릭스 못 읽음 | `<td role="cell" aria-label={`${oem} ${country}: ${value} 대`}>` |
| P1 | 5월 8일 F-1, F-2, F-3 잔존 | StickyTable 키보드 정렬 불가, 토글 `aria-pressed` 없음, 행 onClick 키보드 미대응 | 5월 8일 권고 동일 |
| P2 | `components/oem/Top30YtdChart.tsx`, `Top40YearlyTable.tsx` | 표 `<caption>` 없음 — screen reader가 표의 의미 모름 | `<caption className="sr-only">2026년 YTD TOP30 OEM 판매량</caption>` |
| P2 | OEM 차트들의 색상 사용 | 색상만으로 OEM 구분 (10가지 hex). 색맹 친화성 낮음 | 차트 외 라벨 표기로 보강 (이미 Legend, YAxis dataKey="name" 사용 중 ✅) |

### G. 성능 ❌

| 우선순위 | 위치 | 지적 | 권고 |
|---|---|---|---|
| **P0** | `app/oem/page.tsx:46` | `fetchAll(supabase, 'oem_sales_group_country_month')` — 117,501행을 매 요청 fetch + RSC payload + client 전달 | RPC `get_oem_country_top_matrix(year_start, year_end)` 추가 또는 view 사전 집계로 작은 결과만 받기 |
| P1 | `app/oem/page.tsx:20-37` fetchAll | 117 페이지네이션 = 117번 round-trip (Supabase REST), 응답 9초 | 단일 RPC 또는 batch fetch (Supabase의 `count: 'exact'` + 정확한 chunk) |
| P1 | OEM 차트 11개 모두 `<ResponsiveContainer width="100%" height={...}>` | 페이지에 11개 ResponsiveContainer 동시 마운트 — recharts ResizeObserver overhead | 차트 동적 import 또는 가시성 기반 lazy mount (`react-intersection-observer`) |
| P1 | `components/oem/OemCountryHeatmap.tsx:17-53` | 117K 행 받아서 client에서 두 번 iterate (oem/country total + cell map) — 매 마운트마다 | server 컴포넌트에서 미리 가공한 10×10 매트릭스만 전달 |
| P2 | 5월 8일 G-3 잔존 | next/image 미사용 (CustomerBadges) | 5월 8일 권고 동일 |
| P2 | `scripts/_*.json`, `scripts/_debug_*.html` 커밋됨 | 디버그 캐시 산출물이 repo에 (~수MB) — clone 비용 ↑ | `.gitignore`에 추가 + `git rm --cached` (단, `_marklines_top100.json` 등 시드 데이터는 별도 보존 결정 필요) |

### H. 에러 핸들링·로깅 ⚠️

| 우선순위 | 위치 | 지적 | 권고 |
|---|---|---|---|
| P1 | `app/oem/page.tsx:28-31` fetchAll | error 시 `return all` — 부분 결과로 진행 — **이번 세션의 max-rows=1000 버그가 정확히 이 경로로 잠복** (KPI 모두 0인데 페이지는 정상 응답) | error throw + error.tsx |
| P1 | 5월 8일 H-2/3/4/5 잔존 | NewsModal Supabase error 무시, try/catch 빈 catch, /api/news/search 실패 시 status 200 + 빈 items, lib/logger 미사용 | 5월 8일 권고 동일 |
| P1 | `scripts/import_oem_sales.py:158-180` | 4개 upsert 호출이 throw 시 중간 단계에서 멈추지만 retry/rollback 없음 — 불완전 적재 가능 | trans wrapper 또는 atomic flag 도입 |
| P2 | `components/oem/*.tsx` 차트 | 데이터가 비어있으면 일부 차트는 차트 자체가 안 보이거나 작은 영역으로 잘림 (Top30YtdChart는 "2026년 데이터 없음" 메시지 ✅, 다른 차트는 noop) | 모든 차트에 `if (!data.length) return <EmptyState />` 일관 적용 |

### I. 보안 ❌

| 우선순위 | 위치 | 지적 | 권고 |
|---|---|---|---|
| **P0** | `supabase/migrations/20260510000001_create_oem_sales.sql` 전체 | OEM 5개 테이블 모두 RLS enable + 정책 한 줄도 없음 — anon key가 client에 노출된 환경에서 외부 무방비 | 모든 테이블에 `ALTER TABLE ... ENABLE ROW LEVEL SECURITY;` + `CREATE POLICY "public_read" FOR SELECT USING (true);` |
| **P0** | `supabase/migrations/20260508000008_create_parts_top100_view.sql` | view 생성 시 `security_invoker` 미명시 (5월 8일 I-1과 동일 패턴) | `CREATE OR REPLACE VIEW ... WITH (security_invoker = true)` |
| **P0** | 5월 8일 I-1 / J-1 **잔존** | anon key 노출 환경의 RLS 정책 점검 미수행 (psql `pg_policies` 미확인) | `select * from pg_policies where schemaname='public'` 즉시 실행 + 보고서화 |
| P1 | 5월 8일 I-2, I-3, I-4, I-5, I-6 잔존 | /api/news/search Zod, rate limit, UA 위장, next/image, lib/logger server-only — 모두 미수정 | 5월 8일 권고 동일 |
| P2 | `scripts/_marklines_*.json`, `scripts/_namu_logos.json` 커밋됨 | 외부 사이트 크롤링 결과 — 일부에 사이트별 식별 정보 포함 가능 (Marklines 인증 토큰 등) | 내용 점검 후 ignore 처리, 토큰류 있으면 `git filter-repo`로 히스토리 정리 |

### J. DB·migration·트랜잭션 ⚠️

| 우선순위 | 위치 | 지적 | 권고 |
|---|---|---|---|
| **P0** | I 카테고리 P0 RLS 누락과 동일 | 신규 5개 테이블 + 1개 view 모두 RLS 미설정 | I 권고 동일 |
| P1 | `supabase/migrations/20260510000001_create_oem_sales.sql:14-29` | `oem_sales_monthly` raw 테이블 정의 + 인덱스 3개 — **하지만 `scripts/import_oem_sales.py`는 raw 테이블에 적재하지 않음 (file:14 주석으로 명시)** → 영구히 빈 테이블 + 사용 안 되는 인덱스 | raw 테이블 마이그레이션을 별도로 분리하거나, 이번 마이그에서 제거 |
| P1 | `supabase/migrations/20260508000001~000006` 6종 | UPDATE/INSERT 데이터 정정 마이그 — 트랜잭션 없음 (각 파일은 단일 statement면 안전, 다 statement면 부분 실패 가능) | 각 파일에 `BEGIN; ... COMMIT;` 명시 또는 단일 idempotent UPSERT |
| P2 | `supabase/migrations/20260509000001_normalize_customer_names.sql`, `20260509000002_normalize_customers_v2.sql` | v1 + v2 두 파일 — v1이 v2로 덮어써지는 중복 작업이면 v1 삭제 가능 | 두 파일 의도 차이를 마이그 헤더 주석에 명시 또는 v1 제거 |
| P2 | OEM 4개 사전 집계 테이블 | view 대신 테이블 — 사용자(import_oem_sales.py)만 갱신 책임 짐 | 향후 raw 테이블 적재 시 trigger / materialized view로 자동 갱신 검토 |

### K. 반응형 & UI 일관성 ⚠️

| 우선순위 | 위치 | 지적 | 권고 |
|---|---|---|---|
| P1 | `components/oem/OemDashboard.tsx:33` | `max-w-[1600px]` 고정 + 좌측 사이드바 — 작은 노트북(1280px)에서 일부 차트 좁아짐 | 차트별 minWidth 검증 또는 max-w를 vw로 |
| P1 | `components/oem/Top30YtdChart.tsx:47` | `lg:grid-cols-[1fr_500px]` — 1024px 미만에서 표/차트 위아래 누적, 표 길이 800px가 페이지 늘어짐 | 모바일에서 표를 collapsible accordion 또는 tab 분리 |
| P1 | `components/oem/OemCountryHeatmap.tsx` | 10×10 매트릭스 모바일에서 가로 스크롤만 — 정보 파악 어려움 | 모바일에서 OEM 단일 선택 + 국가별 가로 막대 형태 fallback |
| P2 | OEM 차트 fixed height (예: 420, 481, 660) | 작은 화면에서 비례 축소 안 됨 | aspectRatio 기반 또는 viewport 기반 |

### L. 프로젝트 컨벤션 준수 ⚠️

| 우선순위 | 위치 | 지적 | 권고 |
|---|---|---|---|
| P1 | `components/oem/Top30YtdChart.tsx:25-109` (110줄), `Top40YearlyTable.tsx`, `Top10AnnualBars.tsx`, `Top10MonthlyLines.tsx`, `EvLeadersChart.tsx`, `YoyWinnersLosers.tsx`, `OemCountryHeatmap.tsx` | 모두 80~150줄, 30줄 컨벤션 위반 | 데이터 변환 분리 (B 카테고리와 연동) |
| P1 | 5월 8일 L-1, L-2, L-3, L-4, L-5 잔존 | StockRow/DomesticRow 200줄, getSortValue 50줄, /api/news/search lib/logger 미사용 | 5월 8일 권고 동일 |
| P2 | `components/oem/Top30YtdChart.tsx:74`, `Top10AnnualBars.tsx`, 등 | `<Cell key={i}>` index를 key로 사용 | `key={entry.name}` (oem_group은 unique) |
| P2 | `components/oem/Top30YtdChart.tsx:22` (`TOP_N = 30`), `Top40YearlyTable.tsx`, `Top10AnnualBars.tsx` (`TOP_N = 10`), 등 | 각 파일마다 매직넘버 분산 | `components/oem/constants.ts`에 통합 |
| P2 | `components/oem/PowertrainTopOems.tsx:13-15` | `YEAR_START = 202501`, `YEAR_END = 202512` 등 연도 매직넘버 | `LATEST_FULL_YEAR = 2025` 같은 상수 + 동적 계산 |
| P2 | `components/oem/helpers.ts:138-149` `OEM_COLORS` | 10개 hex 하드코딩 — 다크 모드에서 background 변경 시 채도 차이 미검증 | shadcn 테마 토큰 또는 axe DevTools contrast 검증 |
| P2 | 5월 8일 L-6, L-7, L-8, L-9 잔존 | SUPPORTED_YEARS 하드코딩, `<li key={i}>` index, eslint-disable img, server-only 누락 | 5월 8일 권고 동일 |

---

## 3. 우선 조치 권고 (Top 5)

1. **🔴 P0 — Supabase RLS 정책 일괄 점검·추가** (`I`/`J`)
   - 5월 8일 I-1 + 신규 OEM 5개 테이블 + parts-top100 view 통합
   - `select * from pg_policies where schemaname='public'`로 현재 상태 출력
   - 모든 anon read 허용 테이블에 `ENABLE RLS` + `CREATE POLICY "public_read" FOR SELECT USING (true)` 추가 마이그레이션
   - **이유**: anon key가 client에 노출 + 신규 5개 테이블이 RLS 미설정으로 외부 무방비. **악화** 추세.

2. **🔴 P0 — /oem 페이지 데이터 fetch 최적화** (`G`/`C`)
   - `app/oem/page.tsx:46`의 `groupCountryMonth` 117K 행 client 직렬화 제거
   - 서버 컴포넌트에서 미리 사전 가공: `topCountries(2025)`, `oemCountryMatrix(top10×top10)` 등
   - 또는 RPC 함수 `get_oem_country_matrix(year_start, year_end, top_n)` 추가
   - **이유**: 매 요청 9초 응답 + RSC payload ~5MB. 사용자 경험 + 비용 모두 악화.

3. **🔴 P0 — Table·Row 공통화 / formatRateLabel 추출** (`B`)
   - 5월 8일 B-1/2/3 **모두 잔존** — parts-top100이 DomesticTable을 부분 재사용했지만 StockTable과는 여전히 중복
   - `BaseStockTable<R, K>` + `useStockTableState` hook + 셀 헬퍼 분해 + `lib/exchangeRates.ts`
   - **이유**: 600줄+ 중복이 그대로. OEM 페이지 추가 후 유지보수 표면 더 늘어남.

4. **🟡 P1 — `error.tsx`/`loading.tsx` 4개 라우트 일괄 추가 + viewErr throw** (`C`/`H`)
   - `app/related-stocks/`, `app/domestic/`, `app/oem/`, `app/parts-top100/` 4곳
   - 각 page.tsx에서 `if (viewErr) throw new Error(...)` + fetchAll의 partial return 제거
   - **이유**: 5월 8일 잔존 + 신규 페이지도 동일 결함. 사용자가 빈 차트/표 보고 원인 모름. (이번 세션 max-rows=1000 버그가 정확히 이 결함 경로로 잠복)

5. **🟡 P1 — OEM 차트 컴포넌트 30줄 컨벤션 + 공통화** (`B`/`L`)
   - 차트 컴포넌트별 데이터 변환을 `helpers.ts`로 분리
   - `<HorizontalBarChart>` / `<DualPanel>` 헬퍼 추출 (4개 차트 중복 → 1)
   - **이유**: 13개 차트 모두 80~150줄, 향후 차트 추가 시 또 같은 boilerplate 반복.

---

## 4. 카테고리 외 발견 사항

- **`scripts/_*.json` 디버그/캐시 커밋**: `_marklines_match.json`, `_top100_websearch_log.json` 등이 repo에 들어옴. 일부는 시드/재현 데이터(가치 있음), 일부는 1회성 로그. .gitignore + git rm --cached 분리 정책 필요.
- **`scripts/_debug_*.html`**: Marklines 페이지 캡처 HTML 2개 (~수MB) — 디버깅 후 삭제 대상.
- **`scripts/import_oem_sales.py`의 raw 테이블 미적재**: 마이그레이션엔 `oem_sales_monthly` 정의 + 3개 인덱스가 있으나 스크립트는 적재 안 함. 부분 dead code → 마이그에서 제거하거나 스크립트에 옵션 추가.
- **PowerTrain 정규화 정책**: `EV/PHV` → `EV` 우선순위 매핑이 마케팅 관점(전동화 강조)에는 맞지만 회계/판매 분석 관점에는 PHV로 분류해야 할 수도. 비즈니스 의도 명시 필요.
- **`scripts/debug_oem_page.py`** 커밋: Playwright 검증 스크립트 — 운영과 무관. 추후 e2e 테스트 디렉토리로 이동 검토 (`tests/e2e/oem.spec.ts`).
- **OEM 차트들 모두 `text-xs` className을 recharts SVG에 적용**: SVG 안에서 Tailwind 클래스가 적용 안 될 수 있음 (실제 폰트 크기는 `tick={{ fontSize: 11 }}`로 별도 지정해 OK). className은 무의미한 dead code.
- **5월 8일 보고에 "**현 진단**" 가능 항목**: 이번 세션 max-rows=1000 버그가 fetchAll의 H-1 (error 무시) 경로로 잠복했던 사례 → 5월 8일 H 카테고리 권고가 옳았음을 증명.
- **테스트 파일 부재**: 5월 8일 동일 + OEM `helpers.ts` (집계/순위 순수 함수) 단위 테스트 적합 대상.

---

## 5. 5월 8일 잔존 지적 검증 결과

`review/claude-2026-05-08.md` + `review/codex-2026-05-08.md`의 P0/P1 지적 항목별 검증.

### Claude 5월 8일 P0 (4개)

| ID | 지적 | 5월 10일 상태 | 비고 |
|---|---|---|---|
| B-1 | StockTable ↔ DomesticTable 공통화 (~400줄 중복) | ❌ **잔존** | parts-top100이 DomesticTable 재사용했지만 StockTable과는 여전히 중복 |
| B-2 | StockRow ↔ DomesticRow 공통화 (~150줄 중복) | ❌ **잔존** | 5월 10일 components/domestic/* 수정 있었으나 분리 작업 아님 |
| B-3 | formatRateLabel 중복 | ❌ **잔존** | 두 FilterBar 모두 그대로 |
| I-1 / J-1 | RLS 정책 점검 | ❌ **악화** | 신규 OEM 5개 테이블 + parts-top100 view도 RLS 미설정으로 추가됨 |

### Claude 5월 8일 P1 핵심 잔존 검증

| ID | 지적 | 5월 10일 상태 |
|---|---|---|
| A-1 | Zod 스키마 / 자동 타입 | ❌ 잔존 |
| A-2 | env 비-null 단언 | ❌ 잔존 |
| A-3 | Supabase 응답 `as` 단언 | ❌ 잔존 + OEM 신규에도 동일 |
| C-1 | force-dynamic + Cache Components 미활용 | ❌ 잔존 + OEM/parts-top100도 동일 |
| C-2 | error.tsx 누락 | ❌ 잔존 + OEM/parts-top100도 누락 |
| C-3 | loading.tsx 누락 | ❌ 잔존 + OEM/parts-top100도 누락 |
| C-4 | viewErr throw 안 함 | ❌ 잔존 + OEM fetchAll partial return으로 동일 결함 |
| F-1 | StickyTable 키보드 정렬 | ❌ 잔존 |
| F-2 | 토글 aria-pressed | ❌ 잔존 |
| F-3 | 행 onClick 키보드 미대응 | ❌ 잔존 |
| G-1 | exchange_rates_live 캐시 | ❌ 잔존 |
| H-2 | NewsModal Supabase error 무시 | ❌ 잔존 |
| H-3 | NewsModal 빈 catch | ❌ 잔존 |
| H-4 | /api/news/search 실패 시 status 200 | ❌ 잔존 |
| H-5 | /api/news/search lib/logger 미사용 | ❌ 잔존 |
| I-2 | /api/news/search Zod 검증 | ❌ 잔존 |
| I-3 | /api/news/search rate limit | ❌ 잔존 |
| L-1, L-2 | StockRow/DomesticRow 200줄 | ❌ 잔존 |
| L-5 | /api/news/search lib/logger | ❌ 잔존 (H-5와 동일) |

### Codex 5월 8일 P1 잔존 검증

| ID | 지적 | 5월 10일 상태 |
|---|---|---|
| A | Supabase 결과 `as` 단언 | ❌ 잔존 |
| A | env 비-null 단언 | ❌ 잔존 |
| C | force-dynamic 페이지 loading.tsx/error.tsx | ❌ 잔존 + OEM/parts-top100도 동일 |
| E | /api/news/search Zod | ❌ 잔존 |
| F | StickyTable 키보드 정렬 | ❌ 잔존 |
| F | 행 keyboard/focus | ❌ 잔존 |
| I | RLS / view security_invoker | ❌ 잔존 + OEM/parts-top100도 동일 |

### 5월 8일 → 5월 10일 사이 수정된 항목 ✅

(claude/codex 양쪽 P0/P1 지적 중) **없음**.

5월 8일 ~ 5월 10일 사이 commit 3개 (`c31d861`, `f738cb8`, `34f4a62`)는 모두 **신규 기능 추가**였고, 5월 8일 리뷰 지적의 후속 수정 commit은 발견되지 않음. 신규 페이지(OEM, parts-top100)도 같은 패턴(force-dynamic + as 단언 + viewErr logger only + error.tsx 부재)을 답습함.

---

_2차 Codex 리뷰는 동일 SCOPE/CHECKLIST 기준 + 본 결과 + 5월 8일 결과를 컨텍스트로 별도 진행 → `review/codex-2026-05-10.md` 저장 → `review/diff-2026-05-10.md`에 비교 정리 (사람 작성)._
