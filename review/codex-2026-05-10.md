현재 세션이 `sandbox read-only`라 `review/codex-2026-05-10.md` 파일은 생성하지 못했습니다. 아래가 바로 붙여 넣을 수 있는 리뷰 결과입니다. `npm run typecheck`/`npm run lint`도 현재 정책에서 실행이 차단되어 정적 코드 확인만 했습니다.

```markdown
# 코드 리뷰 - SCOPE-2026-05-10 (Codex)
- 리뷰일: 2026-05-10
- 모델: Codex gpt-5.5
- 범위: review/SCOPE-2026-05-10.md Tier 1 중심 + 2026-05-08 P0/P1 재확인
- 참고: Next.js 16 로컬 문서 확인. cacheLife/cacheTag stable, loading.tsx/error.tsx 스트리밍/에러 경계 권장.

## 1. 요약
- 결과: **P0 1건 / P1 9건 / P2 5건**
- 주요 risk Top 3:
  1. 신규 OEM 테이블 5개가 RLS/policy 없이 생성됨.
  2. `/oem` 데이터 fetch 실패가 부분 데이터/빈 차트로 조용히 렌더링됨.
  3. 5월 8일 P1 다수, 특히 route loading/error, Zod 검증, view security_invoker, a11y가 미해결.

## 2. 체크리스트별 이슈

### A. 타입 안정성
| 심각도 | 위치 | 문제 | 제안 |
|---|---|---|---|
| P1 | `app/oem/page.tsx:17`, `app/oem/page.tsx:33` | `fetchAll<T>(table: string)` + `data as T[]`라 테이블명/응답 shape 불일치를 컴파일러가 못 잡음 | Supabase generated `Database` 타입으로 테이블명을 제한하거나 Zod parse |
| P1 | `app/related-stocks/page.tsx:23`, `app/domestic/page.tsx:22`, `app/parts-top100/page.tsx:22` | Supabase view 결과를 DTO로 직접 cast | view별 schema parse 또는 generated Views 타입 사용 |
| P2 | `lib/supabase/server.ts:9`, `lib/supabase/client.ts:6` | env non-null assertion | env schema로 fail-fast |

### B. 컴포넌트 구조
| 심각도 | 위치 | 문제 | 제안 |
|---|---|---|---|
| P1 | `components/oem/OemDashboard.tsx:9`-`21` | 13개 섹션과 Recharts 컴포넌트를 한 Client Component에서 정적 import | 차트별 dynamic import/Suspense 또는 섹션 lazy loading |
| P2 | `components/related-stocks/StockTable.tsx:38`, `components/domestic/DomesticTable.tsx:19` | `resolveLatestYear`, `buildColumns`, `getSortValue` 중복이 유지됨 | 공통 table hook/column factory로 축소 |
| P2 | `components/related-stocks/FilterBar.tsx:29`, `components/domestic/DomesticFilterBar.tsx:23` | `formatRateLabel` 중복 | `lib/format.ts`로 이동 |

### C. Next.js 16
| 심각도 | 위치 | 문제 | 제안 |
|---|---|---|---|
| P1 | `app/related-stocks/page.tsx:6`, `app/domestic/page.tsx:6`, `app/parts-top100/page.tsx:6`, `app/oem/page.tsx:11` | 모두 `force-dynamic`; Cache Components/`use cache`/`cacheTag` 미사용 | 갱신 주기가 있는 데이터는 cache tag 기반으로 전환 |
| P1 | `app/` 전체 | `loading.tsx`, `error.tsx`, `not-found.tsx`가 없음 | route별 skeleton/error boundary 추가 |
| P1 | `app/oem/page.tsx:28`-`31` | Supabase 오류를 throw하지 않고 partial `all` 반환 | 실패 시 throw 후 `error.tsx`에서 처리 |

### F. 접근성
| 심각도 | 위치 | 문제 | 제안 |
|---|---|---|---|
| P1 | `components/common/StickyTable.tsx:165`-`182` | `<th onClick>` 정렬은 키보드 조작/`aria-sort` 없음 | 내부 `<button>` + `aria-sort` |
| P1 | `components/related-stocks/StockRow.tsx:101`-`107`, `components/domestic/DomesticRow.tsx:111`-`117` | 클릭 가능한 `<tr>`에 focus/keyboard handler 없음 | 토글 버튼 분리 또는 `tabIndex`/Enter/Space 처리 |
| P2 | `components/common/ToggleFilterBar.tsx:36`-`43`, `components/domestic/DomesticFilterBar.tsx:148`-`155` | toggle button에 `aria-pressed` 없음 | active 상태 반영 |

### G. 성능
| 심각도 | 위치 | 문제 | 제안 |
|---|---|---|---|
| P1 | `components/oem/*Chart.tsx` | Recharts 10개 컴포넌트가 `/oem` 첫 번들에 정적으로 포함 | `dynamic(() => import(...), { ssr:false })` 검토 |
| P2 | `app/oem/page.tsx:26` | `select('*')`로 집계 테이블 전체 컬럼 fetch | 필요한 컬럼 명시 |
| P2 | `components/related-stocks/CustomerBadges.tsx:24`-`26` | raw `<img>` + lint disable | `next/image`와 `images.remotePatterns` 설정 |

### H. 오류 처리/로깅
| 심각도 | 위치 | 문제 | 제안 |
|---|---|---|---|
| P1 | `app/api/news/search/route.ts:39`, `app/api/news/search/route.ts:77`-`78` | upstream 실패/catch가 모두 200 + 빈 배열, logger 없음 | 4xx/5xx 구분, pino 로그, client fallback 분리 |
| P1 | `components/related-stocks/NewsModal.tsx:71`-`77` | Supabase error를 읽지 않음 | `{ data, error }` 확인 후 사용자 상태/toast |
| P2 | `components/related-stocks/NewsModal.tsx:128`, `app/stock-popup/[id]/page.tsx:130` | invalid/null date 방어 없음 | safe date formatter 사용 |

### I. 보안
| 심각도 | 위치 | 문제 | 제안 |
|---|---|---|---|
| **P0** | `supabase/migrations/20260510000001_create_oem_sales.sql:14`, `:34`, `:46`, `:60`, `:74` | 신규 OEM 5개 테이블에 RLS enable/policy가 없음 | 모든 OEM 테이블에 RLS enable, anon SELECT, service_role ALL policy 명시 |
| P1 | `supabase/migrations/20260507000006_add_domestic_page_support.sql:59`, `:126`, `supabase/migrations/20260508000008_create_parts_top100_view.sql:7` | view가 `security_invoker=true` 없이 생성됨 | `CREATE VIEW ... WITH (security_invoker=true)` |
| P1 | `app/api/news/search/route.ts:69`-`75` | query Zod 검증/rate limit 없음 | `q max`, `country enum`, rate limit 추가 |

### J. DB/migration
| 심각도 | 위치 | 문제 | 제안 |
|---|---|---|---|
| P1 | `supabase/migrations/20260510000001_create_oem_sales.sql:14`-`82` | migration이 테이블/index만 만들고 privilege/RLS/comment 검증 없음 | RLS/policy/comment/GRANT 상태를 migration에 포함 |
| P2 | `supabase/migrations/20260508000008_create_parts_top100_view.sql:7` | view drop/create가 권한/보안 속성을 보존하지 않음 | view 생성 패턴 표준화 |

## 3. 2026-05-08 P0/P1 재확인
- Claude P0 `formatRateLabel` 중복: **미해결** (`FilterBar.tsx:29`, `DomesticFilterBar.tsx:23`)
- Claude/Codex P1 `loading.tsx`/`error.tsx`: **미해결** (`app`에 해당 파일 없음)
- Claude/Codex P1 Supabase cast: **미해결** (`page.tsx` cast들 유지)
- Claude/Codex P1 `/api/news/search` Zod/rate-limit/logger: **미해결**
- Claude/Codex P1 table header a11y: **미해결**
- Claude P0 RLS 점검: 기존 주요 테이블은 RLS 있음, 하지만 **5/10 신규 OEM 테이블에서 새 P0 발생**

## 4. Top 5 수정 권장
1. **P0** `20260510000001_create_oem_sales.sql` OEM 5개 테이블 RLS/policy 추가.
2. **P1** `/oem` fetch 실패 시 throw + route `error.tsx`/`loading.tsx` 추가.
3. **P1** Supabase 응답 cast 제거: generated Database 타입 또는 Zod parse 도입.
4. **P1** `/api/news/search` Zod 검증, rate limit, logger, 실패 status 정리.
5. **P1** `StickyTable` 정렬 헤더와 클릭 행 a11y 수정.
```

검증 제한: read-only 정책 때문에 파일 작성, `npm run typecheck`, `npm run lint`는 실행되지 않았습니다.