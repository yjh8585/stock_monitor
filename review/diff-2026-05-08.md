# 리뷰 비교 정리 — Claude vs Codex (2026-05-08)

- **1차 Claude** (`review/claude-2026-05-08.md`): P0 **4** / P1 **26** / P2 **18**
- **2차 Codex** (`review/codex-2026-05-08.md`): P0 **0** / P1 **5** / P2 **8**

> 본 문서는 두 리뷰의 **공통점·차이점·충돌**을 분류하고, **다음 PR 분할 권고**를 정리한다.

---

## 1. 공통 지적 (양쪽이 P0 또는 P1로 지적) — 우선 수정 대상 ✅

| # | 항목 | Claude | Codex | 카테고리 |
|---|---|---|---|---|
| 1 | Supabase 응답 타입 단언 제거 (`as RelatedStockRow[]` 등) | P1 | P1 | A |
| 2 | 두 페이지에 `loading.tsx`/`error.tsx` 추가 | P1 | P1 | C |
| 3 | `/api/news/search` Zod 입력 검증 + 길이/enum 제한 | P1 | P1 | I·E |
| 4 | `<th>` 정렬 헤더 키보드 접근성 (`<button>` or role/tabIndex/onKeyDown) | P1 | P1 | F |

> 위 4건은 **양쪽이 동시에 P1**로 합의한 항목. **다음 PR에서 우선 수정**.

### 양쪽 P2 일치 (시간 될 때)
- `lib/logger.ts` `import 'server-only'` 추가
- `view select('*')` → 명시 컬럼
- `<img>` next/image 회피 (`next.config.ts` `images.remotePatterns` 추가)
- 30줄 컨벤션 위반(Row 200줄·Table 50줄) → 셀 단위 분리

### 우선순위 차이 (한쪽 P1, 다른쪽 P2) — 사람 판단
- `lib/supabase/server.ts` env non-null 단언 (Claude P1 / Codex P2) — **결정 필요**
- 30줄 컨벤션 (Claude P1 / Codex P2) — Claude 입장 채택 권고

### `aria-*` 속성 (관점 보완)
- Claude: `aria-pressed`(토글 버튼) 강조
- Codex: `aria-label`(아이콘 버튼), `aria-sort`(정렬 헤더) 강조
- → **둘 다 반영**해야 완성. 카테고리 F의 큰 작업 묶음.

---

## 2. 한쪽만 지적 — 사람이 판단

### 2-1. Claude만 발견 (Codex가 놓친 부분)

| 항목 | Claude 우선 | 판단·조치 |
|---|---|---|
| **DRY 위반 — `StockTable`/`DomesticTable`, `StockRow`/`DomesticRow` ~600줄 중복** | **P0** | ✅ 수긍. 새 페이지(글로벌·중국) 추가 시 큰 부담. **우선 수정 권고**. 단 큰 작업이라 PR 분할(아래 Step F). |
| **`formatRateLabel` 함수 중복** | **P0** | ✅ 수긍. 한 줄짜리 분리라 즉시 가능. **Step A로 빠른 정리**. |
| **`openCompanyLink` 함수 중복** (StockRow vs DomesticRow) | P1 | ✅ 수긍. **Step A로 함께**. |
| **`viewErr` throw → `error.tsx`로 fallback** | P1 | ✅ 수긍. 단순 throw로 사용자 에러 노출 가능. **Step B**. |
| **`NewsModal.tsx:91-93` catch 빈 본문** | P1 | ✅ 수긍. 디버깅 어려움. **Step B**. |
| **`NewsModal.tsx:71-77` Supabase 쿼리 error 무시** | P1 | ✅ 수긍. **Step B**. |
| **`lib/types.ts` Zod 스키마 도입** | P1 | ⚠️ 결정 필요. Codex의 "generated types" 의견과 결합 가능: generated types로 1차 안전망 + 외부 입력만 Zod parse. |
| `resolveLatestYear` 매직 데이터 `['2026',...]` 하드코딩 | P2 | ✅ 수긍. 상수 추출 or fy 키에서 동적 추출. |
| `<th>` 동일 `key: 'name_kr'` 3개 컬럼 → 정렬 의도와 다름 | P2 | ✅ 수긍. `StickyColumn`에 `sortable?: boolean` 추가. |
| `useToggleArray<T>` hook 추출 | P2 | 🟢 시간 되면. Step F의 일부. |

### 2-2. Codex만 발견 (Claude가 놓친 부분)

| 항목 | Codex 우선 | 판단·조치 |
|---|---|---|
| **`migrations/...add_domestic_page_support.sql` `company_pages` anon read 전체 허용** (구체 라인) | **P1** | ⚠️ **검증 필수**. Claude는 일반론으로 RLS P0, Codex는 구체 라인 P1. 실제 정책 점검 후 결정 (Step C). |
| **migration 트랜잭션 누락** | P2 | ⚠️ 검증: Supabase migration CLI가 자동으로 BEGIN/COMMIT 감싸는지 확인. 이미 자동이면 무시 가능. |
| **view `security_invoker` 옵션 미명시** | P2 | ⚠️ Postgres 15+ view 보안 옵션. RLS와 함께 검토 (Step C). |
| **`finally setLoading(false)` 누락** (`NewsModal.tsx:71-95`) | P2 | ✅ 수긍. catch 시 로딩 무한 가능. **Step B**에 포함. |
| `<th> aria-sort`, 아이콘 버튼 `aria-label` | P1·P2 | ✅ 수긍. **Step D**에 흡수. |

---

## 3. 의견 충돌·관점 차이

### 3-1. DRY 위반 우선순위
| 측면 | 입장 |
|---|---|
| Claude | P0 — 즉시 수정. 두 페이지 + 향후 추가 페이지 비용 폭발. |
| Codex | P2 — 가볍게 언급. |
| **판단** | **Claude 입장 채택**. 단 한 번에 리팩토링하지 않고 Step A→F 순으로 분할. |

### 3-2. RLS 관점
| 측면 | 입장 |
|---|---|
| Claude | P0 — `pg_policies` 광범위 점검. anon key 노출 + RLS 누락 시 데이터 무방비. |
| Codex | P1 — 구체 migration 라인 (`company_pages` anon read) + `security_invoker` 추가. |
| **판단** | **두 관점 결합**. `pg_policies` 전수 점검(Claude) + 구체 라인 검증(Codex) 동시 진행. |

### 3-3. P0 개수 차이 (4건 vs 0건)
| 측면 | 해석 |
|---|---|
| Claude | 구조적 위험을 P0로 강조. |
| Codex | 보수적 우선순위, 모든 항목을 P1·P2로. |
| **판단** | Claude의 P0 4건은 본 프로젝트의 단계(/관련주식·국내자동차 + 향후 페이지 확장)에 맞춰 **타당**. 단 한 번에 해결 강박 X — Step별 PR로 분할. |

---

## 4. 권장 다음 단계 — 6 PR 분할 안

각 PR은 review·테스트 가능한 단위로 분리.

### 🎯 Step A — 빠른 정리 (1 PR, 1~2시간)
> 대부분 이동·import 수준이라 위험 낮음.
- [ ] `formatRateLabel` → `lib/exchangeRates.ts` (또는 `lib/format.ts`)
- [ ] `openCompanyLink` → `lib/companyLink.ts`
- [ ] `lib/logger.ts`에 `import 'server-only';` 추가
- [ ] `app/api/news/search/route.ts`에 Pino logger import + 실패 로깅
- [ ] `NewsModal.tsx:137` `<li key={i}>` → `<li key={n.url}>`
- [ ] `resolveLatestYear` 매직 데이터 `SUPPORTED_YEARS` 상수 추출

### 🛡 Step B — 안전망 (1 PR, 반나절)
> 사용자 에러 표시 + 에러 무시 제거. UX·디버깅 직결.
- [ ] `app/related-stocks/loading.tsx`, `error.tsx`
- [ ] `app/domestic/loading.tsx`, `error.tsx`
- [ ] 두 `page.tsx`에서 `if (viewErr) throw new Error(...)`
- [ ] `NewsModal`: Supabase `error` 분기, `finally setLoading(false)`, catch에 logger
- [ ] `lib/supabase/server.ts` env Zod 검증 또는 명시 throw

### 🔒 Step C — 보안·DB (1 PR, 반나절~1일, **DB 점검 시간 별도**)
- [ ] `select * from pg_policies where schemaname='public'` 점검
- [ ] 누락된 테이블·view에 read-only RLS 추가
- [ ] `add_domestic_page_support.sql` `company_pages` 정책 재검토
- [ ] view에 `security_invoker = true` 옵션 추가 (Postgres 15+)
- [ ] migration 트랜잭션 자동성 확인 (Supabase CLI 동작 검증)
- [ ] `/api/news/search` Zod 입력 검증 (`q.max(100)`, `country.enum(['kr','global'])`)
- [ ] `/api/news/search` rate limit (Vercel Firewall 또는 Upstash Redis)

### ♿ Step D — 접근성 (1 PR, 반나절)
- [ ] `<th>` button 래핑 또는 role/tabIndex/onKeyDown + `aria-sort`
- [ ] `ToggleFilterBar`/`DomesticFilterBar` 토글 버튼 `aria-pressed={active}`
- [ ] `ProductCell`/`NewsModal`/`ToggleFilterBar` 아이콘 버튼 `aria-label`
- [ ] `DomesticFilterBar` 드롭다운 Esc 키 닫기
- [ ] `CustomerBadges` 폴백 배지에 `aria-label` 또는 `<span class="sr-only">`
- [ ] 테이블에 `<caption className="sr-only">` 추가

### 🧬 Step E — 타입 안정성 (1 PR, 반나절~1일)
- [ ] `supabase gen types` 자동 생성 → `lib/database.types.ts`
- [ ] `lib/types.ts` view DTO를 generated types 기반으로 재정의
- [ ] (선택) 외부 입력 또는 신뢰 안 되는 경계에 Zod parse 도입
- [ ] page.tsx에서 `as ...` 단언 제거
- [ ] `r.base as keyof ExchangeRates` → type guard로 narrowing

### 🏗 Step F — 구조 개선 (큰 PR, **별도 plan 필요**)
> 양이 많아 별도 plan 권고.
- [ ] 셀 헬퍼 추출: `<RevenueCells>`, `<OpMarginCells>`, `<CagrCell>`, `<DebtRatioCell>`, `<InvTurnoverCell>`, `<PriceCell>`, `<MarketCapCell>`, `<ValuationCells>`
- [ ] `BaseStockRow` / `BaseStockTable` 추상화
- [ ] `useStockTableState<R, K>` hook 추출 (sort/filter 상태 일반화)
- [ ] `useToggleArray<T>` hook 추출 (typeFilter/listingFilter/regionFilter 토글 일반화)
- [ ] `DomesticFilterBar` → `ToggleFilterBar` + leftSlot으로 GroupMultiSelect 통합
- [ ] `StickyColumn`에 `sortable?: boolean` 옵션 추가

---

## 5. 잠정 결론

| 측면 | 결론 |
|---|---|
| **Claude vs Codex 신뢰도** | 동등. 관점만 다름. Claude는 **구조·DRY**에 강하고, Codex는 **DB·보안 디테일**에 강함. |
| **즉시 우선** | Step A·B (1~2일 안에 가능) |
| **일정 잡고 진행** | Step C·D·E (각 반나절~1일) |
| **별도 plan으로 진행** | Step F (큰 리팩토링) |
| **본 리뷰 후 다음 회차** | Step F 종료 후 같은 체크리스트로 재리뷰 권고 |

---

_작성: 2026-05-08 / 1차 Claude · 2차 Codex 리뷰 비교._
