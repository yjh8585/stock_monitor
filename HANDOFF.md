# HANDOFF

다음 세션이 그대로 이어받기 위한 인수인계 기록. **맨 위가 최신**이고, 새 블록을 쓸 때
직전 블록은 `## 이전 상태 …` 로 강등한다.

**여기엔 최근 10블록만 둔다.** 그보다 오래된 블록은 지우지 않고
[`docs/handoff-archive-2026.md`](./docs/handoff-archive-2026.md)(현재 **4블록**)로 옮긴다.

---

## 최신 상태 · 재개 지점 (2026-09-16) — 국내 상장사 회사설명 180곳을 fnguide 정본으로 되돌리고 덮어쓰기를 막았다

계획서 = `docs/superpowers/plans/2026-09-16-company-summary-recovery.md` (+ `_결정이행.md`)

**사용자 지적 세 가지에서 시작했다** — ①휴머노이드 페이지 회사 정보가 이상하다(하이젠알앤엠)
②퓨트로닉 제품이 이상하다 ③전반적으로 제대로 수집했는지 확인해 달라.

### 무엇이 문제였나

- 국내 상장사 **180곳 전부** `business_summary` 가 fnguide 원문이 아니었다(생존 0곳).
  fnguide 수집기는 멀쩡했고 2026-07-14 에 170건 정상 수집돼 있었는데 **사흘 뒤 덮였다.**
- 범인 ① 2026-07-17 `enrich_description_v2.py` 가 폐지된 구 fnguide 도메인을 봤다.
  폐지 도메인은 **404 가 아니라 HTTP 200 안내 페이지**라 「내용 없음」으로 읽혀 2차 LLM
  폴백이 166곳을 덮었다. 도메인은 3주 뒤 고쳤지만 값은 안 되돌렸다.
- 범인 ② `enrich_company.py` 가 **제품·홈페이지가 비었다는 이유**로 대상에 넣고 설명까지
  덮었다(2026-08-24 휴머노이드 보강에서 재발 · 그 16곳만 평균 162자).
- 곁가지: `collect_kr_snapshot.py` 에 revalidate 가 없었다 · 시가총액이 179건 조용히 실패 중.

### 무엇을 했나

- **복구 완료** — 180/180 fnguide 원문(「동사는…」 · 평균 450자). 미수집·폴백·실패 0건.
- **가드** `lib/companies.py` 의 `keeps_existing_summary()` 를 보강 스크립트 5개에 적용.
  값이 **비어 있을 때만** LLM 허용(신규 상장사 공백 방지 · 사용자 결정).
  🔴 조회 컬럼에 `market` 을 빠뜨리면 판정이 전부 False 가 되어 가드가 무동작한다.
- **검사기** `scripts/verify_summary_source.py` 신설(exit 0 정상 · 자가 시험 내장).
- **`collect_kr_snapshot.py`** 를 `WriteSession` 으로 전환(캐시 무효화 누락 수정).
- **휴머노이드 비상장 9곳** 제품·설명을 공식 홈페이지 기준으로 정정. 퓨트로닉은 EV Infra
  추가·설명문이 제품 칸에 있던 것 제거·모터 카테고리 분리·블랙스톤 인수 반영·`ticker` NULL.
- **시가총액** 실패를 침묵시키지 않게 고치고 시장 전체 조회 폴백을 넣었다.

### 재개 지점

1. ✅ **푸시 완료**(`2d28983`). 백업 봇 커밋 위로 리베이스해 올렸다.
2. ✅ **시가총액 복구 완료** — GHA 진단으로 KRX 시총 계열만 깨진 것을 확인하고
   `lib/naver_market_cap.py` 로 교체했다. 워크플로 success · **180/180 시총 확보**
   (NULL 0건). 현대모비스 39.2조 → 34.5조로 교정.
3. 🔴 **화면 육안 확인만 남았다** — `/humanoid` 에서 하이젠알앤엠(설명이 「동사는 2007년
   설립 후 오티스엘리베이터코리아…」인지)과 퓨트로닉(제품에 전기차 충전기·모터·로봇
   액추에이터가 있는지)을 펼쳐 본다. 로그인이 필요해 이번 세션에서 못 했다.
   🔴 **HTTP 상태 코드로 판정하지 말 것** — PPR 이 셸을 먼저 흘려보내 404/200 이
   판정 근거가 안 된다(→ `docs/gotchas-playwright-ui.md`).

## 이전 상태 · 재개 지점 (2026-09-15 밤) — 보고서의 외화 수치에 원화 환산을 못 박고 137번에 반영했다

**무엇을 했나**

- 사용자 지시로 **모든 보고서에서 외화 금액에 원화 환산을 괄호로 병기**하는 규칙을 신설했다(`report.md` §3-B). 지시 원문 2건을 그대로 인용해 뒀다.
- 보고서 **#137**(유럽·미주 파워 스티어링) 본문의 외화 수치 **33곳**에 원화를 병기하고, §9 통화 혼합 비교표 아래에 **원화 환산표와 적용 환율 각주**를 넣었다(52,324자 → 53,754자). 로컬·프로덕션 캐시 무효화 완료.
- 검사기 `scripts/verify_report_krw.py` 신설(자가 시험 포함) · 헬퍼 `scripts/lib/fx.py` 에 `krw_rate_on()`·`krw_text()` 추가 · `AGENTS.md` 검증 명령에 등록.

**결정과 근거**

- 🔴 **환율은 「보고서 작성 시점」 하나만 쓴다**(사용자 추가 지시). 처음엔 내가 회계연도 평균·그달 평균으로 나눠 기준표를 만들었는데, 사용자가 _"어렵게 생각하지 말고 작성시점 환율 그냥 집어넣어. 복잡하게 만들지 마 어차피 환산값인데"_ 로 뒤집었다. 규칙을 그 자리에서 단순화했다(4행 기준표 → 한 줄).
- 🔴 **기존 글은 소급하지 않는다**(사용자 결정). 전수 감사로 114편 중 **60편·525건**이 미병기인 것을 확인했지만 앞으로 쓰는 글에만 적용한다. 그래서 `--all-posts` 건수는 게이트가 아니라 참고 수치다.
- **#137 만 예외적으로 회계연도 평균환율**로 환산돼 있다(각주에 명시). 이미 반영한 뒤 규칙이 단순화돼 방식이 갈렸다 — 통일하려면 다시 돌려야 한다.

**막힌 곳 · 안 되더라**

- 🔴 **검사기 첫 정규식이 한국어 조사에 죽었다.** `달러(?![가-힣])` 로 막았더니 「45.84억 달러**다**」·「106억 유로**를**」이 전부 통과했다. 자가 시험(`MUST_HIT`)이 3건을 잡아 알았다. 지금은 조사를 막지 않고, 한 글자라 위험한 「엔」만 **앞에 단위(억·만·조·천)를 요구**한다(「V8 엔진」 회피).
- 🔴 **`AGENTS.md` 가 상한(36,600B)에 3B 남기고 꽉 차 있었다.** 새 검사기 한 줄을 넣으려고 `pnpm run dev` 함정 서술을 `docs/gotchas-ci-deploy.md` §6-A 로 옮겨 자리를 만들었다(내용 손실 없음 · 현재 36,575B).
- 🔴 **검은 콘솔 창 문제로 사용자에게 두 번 지적받았고 아직 미해결이다.** 첫 진단이 반쪽이었다 — 「PowerShell 도구 대신 Bash 도구」로 바꿨는데도 창이 계속 떴다. 실제 원인은 `~/.claude/docs/gotchas.md` §5 대로 **훅이 분리 실행돼 도구 호출마다 자기 콘솔을 할당**하는 것이다. **도구 선택으로는 안 잡힌다.**
- 원문 불일치 1건 — #137 의 넥스티어 조향 매출이 **본문 35.05억 달러 · §9 표 31.00억 달러**로 갈린다(부문 합계 EPS+HPS 는 33.10억). 원문 충실 원칙대로 둘 다 적힌 대로 환산만 했다.

**재개 지점**

1. **콘솔 창 원인 규명** — 사용자가 `Get-CimInstance Win32_Process -Filter "Name='python.exe'" | Select ProcessId,CommandLine` 로 확인하기로 했다. `hook_*.py` 가 보이면 훅 배선이고 `_tmp_*.py` 면 다른 세션이다(둘 다 전례 있음).
2. 소급을 나중에 하기로 바뀌면 `python scripts/verify_report_krw.py --all-posts` 로 대상을 다시 뽑는다(건수 상위는 마크라인 계열 #107·#138·#79).
3. #137 의 환율 방식을 새 규칙(작성 시점)으로 통일할지 미정.

**바뀐 파일**: `report.md`(§3-B 신설) · `AGENTS.md`(검사기 등록) · `docs/gotchas-ci-deploy.md`(§6-A 신설) · `scripts/lib/fx.py` · `scripts/verify_report_krw.py`(신규) · DB `posts` #137 본문

## 이전 상태 · 재개 지점 (2026-09-15 저녁) — 보고서 PDF 26건을 발행처 링크로 바꾸고 출처 이름을 합쳤다

**무엇을 했나**

- **출처 이름 24건 통일** — `KATECH 한국자동차연구원`→`한국자동차연구원` · `MarkLines Automotive Industry Portal`→`MarkLines` · `Daishin TV`→`대신TV` · `차플레이 Chaplay`→`차플레이` · `내부 분석`·`전략개발실`→`한세모빌리티`. 출처 22종 → 16종.
- **PDF 26건을 `source_url` 로 대체하고 Storage 에서 삭제**(한국자동차연구원 18 · KAMA 5 · KOTRA 1 · ABB 백서 1 · 중복 글 1). `reports` 버킷 216MB → 193MB.
- **중복 게시글 1건 삭제**(id 19 — id 7 과 같은 KAMA 러시아 편. 본문 4,497자 < 7,812자라 짧은 쪽을 지웠다) · **어느 글도 안 쓰던 고아 PDF 2개 삭제**.
- **새 규칙을 문서에 못 박았다** — `docs/report-from-pdf.md` §2-A. PDF 를 받으면 **올리는 게 아니라 출처를 읽고 URL 부터 찾는다**가 기본값이다.

**결정과 근거**

- 🔴 **용량 전제가 실측과 달랐다.** 사용자는 「PDF 가 DB 를 불린다」고 보고 시작했는데, PDF 는 **30개 24MB 로 전체의 4%** 였다. 진짜 덩치는 **`reports-video` 314MB(투란 공장 mp4 한 개)** 와 **`oem_sales_model_country_month` 210MB** 다. 보고하고 진행 여부를 물어 그대로 진행했다.
- **PNG 192MB 는 손댈 게 없다** — 미참조가 **9장 2.9MB** 뿐이다. 🔴 첫 집계에서 `research/` 823장이 「전부 미참조」로 나왔는데 **`research_reports.images` 를 안 봤기 때문**이었다(실제 822/823 참조됨). 대상에서 빠진 것을 「0건」으로 읽을 뻔했다.
- **지우기 전에 반드시 대조했다** — 발행처 PDF 의 **파일명과 바이트수**를 보관본과 맞춰 보고, 둘 다 같은 건만 지웠다(KATECH 18/18 · ABB 4,841,818B 정확히 일치). 사용자 결정은 「공개 링크를 찾은 것만 삭제」였다.

**막힌 곳 / 안 되더라**

- 🔴 **KATECH 목록에서 제목 링크를 긁으면 `href=""` 라 빈 값이다.** 실제 주소는 `<tr data-post_key>` 속성이고 클릭 시 JS 가 `?ac=view&post=<key>` 로 조립한다. 이걸 모르고 **`#vol166` 같은 가짜 앵커**를 넣은 글이 7편 있었다(열어도 목록 첫 화면으로 간다). 이번에 진짜 링크로 바꿨다.
- 🔴 **KAMA 직링크(`FileDown.jsp`)는 세션 쿠키가 없으면 HTTP 200 에 22바이트 빈 응답**이다(파일이 없는 게 아니다). 그래서 상세페이지 URL 을 쓴다. 또 산업동향·policy 게시판은 **유료회원 게이트**가 걸리고 **보도자료(`Bodo`) 만 무료**다.
- **링크를 못 찾아 PDF 를 남긴 2건** — 「2024년 세계 자동차 생산 현황과 시사점」은 KAMA 첨부가 **HWP 뿐**이고, AlixPartners 는 사내 컨설팅 자료다.
- ⚠️ **일회성 대조 스크립트는 버전 관리 밖이다** — `scripts/_prune_katech_pdfs.py` 가 `.gitignore:110` 의 `scripts/_*.py` 에 걸린다. 되살리는 데 필요한 것은 `report-from-pdf.md` §2-A 에 다 적어 뒀다.

**재개 지점**

- 이 갈래는 끝났다. 이어서 할 만한 것은 **용량 실측이 가리킨 두 곳**이다 — `reports-video` 의 **투란 공장 mp4 314MB**(어디서 쓰이는지 확인 후 외부 이전 검토)와 **`oem_sales_model_country_month` 210MB**(구체화 뷰가 이미 있으니 원본 보존 정책을 볼 자리다). 둘 다 **사용자 승인 전**이다.
- 🔴 **앞으로 PDF 를 받으면** `docs/report-from-pdf.md` §2-A 를 먼저 편다. 올리는 것이 아니라 **출처를 읽고 URL 을 찾는 것**이 1번이다.

---

## 이전 상태 · 재개 지점 (2026-09-15) — MarkLines 원문을 쿠키로 받아 게시하고, 번역투를 기계로 막았다

**무엇을 했나**

1. **파워 스티어링 리포트를 게시했다**(post **137** · 52,324자 · 표 22개 · 카테고리 `부품사`).
   MarkLines 「Analysis Report: Power Steering (European and U.S. Markets)」(2026-09-08)을 옮겼다.
   생산·개발 거점 **121개 행을 한 줄도 줄이지 않고** 실었다.
   - 🔴 **원문 URL 은 유료 회원 전용이라 WebFetch 로는 목차만 온다.** 그래서 처음엔
     「소스1만으로는 불가」라고 결론냈는데 **틀렸다** — 이 레포엔 `MARKLINES_COOKIE` 가 있다.
     `requests` 에 실어 보내니 482KB 전문이 왔다(패턴 = `collect_marklines_direct.py`).
   - 🔴 **클리핑(옵시디언)이 잃는 것은 «HTML 속성에 실린 데이터» 다.** 본문 텍스트와 표 셀은
     원문과 완전히 일치했는데, ①**범례 2개**(〇 납품 · ◎ SBW · ☐ 후륜조향 · **빈칸 = 조사 중**)와
     ②`bgcolor="#00B0F0"` 로만 칠해진 매트릭스 표가 통째로 빠져 있었다.
     **「빈칸 = 공급 없음」이 아니라 「미조사」** 라 해석이 뒤집힌다.
   - 색 매트릭스는 글자로 옮길 수 없어 **Playwright headless 로 원문 화면을 캡처**해
     Storage(`reports/marklines-power-steering/eps-assist-matrix.png`)에 올려 본문에 실었다.
     나머지 11개 표는 **일부러 이미지로 만들지 않았다** — 121행 표를 그림으로 넣으면
     검색·복사·모바일 줄바꿈이 죽는다.
2. **번역투를 기계로 막는 검사기를 만들었다** — `scripts/verify_translationese.py`(**exit 0 이 정상**).
   - 발단: 리포트 **136** 에서 영어 `cover` 를 「덮다」로 한 단어씩 치환한 문장이 **5곳** 나갔다
     (「엣지 케이스를 조기에 **덮어야** 하는가」). 한국어에서 「덮다」는 «가린다» 라 의미가
     반대로도 읽힌다. **lint·타입·표 검사 어디에도 안 걸리고** 사용자가 읽고 지적해서 알았다.
   - 규칙을 정하기 **전에** 후보를 넓게 던져 **114편 전수**를 먼저 쟀다. 결과는 「전수 오염이
     아니라 한 편의 문제」였고, 케이스(13건)·가진다(2건)·수행한다(2건)는 **전부 정상**이라
     규칙에서 뺐다. 정밀도 우선으로 **6종만** 본다.
   - 검사기가 **124번**의 「남에게 **보여지는** 가치」(이중 피동)를 새로 찾아냈다 → 「보이는」으로 수정.
3. **136·124 를 고쳤고**, 반영 전 **DB 원본과 diff 를 떠서 바뀐 줄이 예상과 같은지 확인**한 뒤 넣었다.
4. **브레이크 리포트 2편을 더 올렸다**(같은 날 후반 작업).
   - **#138 신규** — 「Brake (Japanese Market)」(2026-06-22) · 39,180자 · **표 24개** · 도해 2장.
   - **#79 교체**(사용자 승인) — 「Brake (U.S. and European Markets)」(2026-03-24) · 21,148 → 37,842자 ·
     표 6 → **14개** · 도해 2장. 같은 `source_url` 로 **2026-05-13 에 이미 글이 있었고**(중복 가드가 잡았다)
     거점·마찰재 표가 통째로 빠진 데다 **카테고리·발행일이 비어 목록 맨 아래로 가라앉아** 있었다.
     🔴 **기존 본문은 되돌릴 수 있게 파일로 백업한 뒤에 UPDATE 했다.**
   - 🔴 **이번에 조용히 유료 벽을 받을 뻔했다** — `MARKLINES_COOKIE` 가 **두 파일에 다른 값**으로 있고
     `.env.local` 쪽은 `_ga`·`_gid` 뿐인 껍데기다. 그걸로 받으면 **HTTP 200 · 34만 자 · `<table>` 0개**가
     온다. `init_script()` 는 `scripts/.env` 를 먼저 읽어 안전하지만, `load_dotenv` 를 직접 부르면
     순서가 뒤집혀 껍데기가 이긴다 → `docs/oem-collection.md` 「쿠키가 «두 곳»에 있고」.
   - **19행×57열 장착 행렬은 축을 뒤집어** OEM×부품 6열로 다시 짰다(정보 손실 없음). 그대로 옮기면
     못 읽고, 이미지로 만들면 검색·복사가 죽는다. 도해 2종만 원본이 GIF 라 PNG 로 변환해 올렸다.
   - 검증: 번역투 **EXIT=0** ×2 · **실제 remark 체인에 통과시켜 raw 별표 0건** · 표 열 수 불일치 0건 ·
     도해 4장 공개 URL HTTP 200(PNG 시그니처 확인) · dev 화면 스크린샷으로 육안 확인.
     ⚠️ **인라인 코드 안에 별표 두 개를 쓰지 말 것** — prettier 가 인접한 굵게 표시와 엉켜
     이 줄을 한 번 망가뜨렸다(`0건**` → `0건\*\*`). 「raw 별표」처럼 말로 쓴다.

**결정과 근거**

- 🔴 **자가 시험을 검사기 «안에» 넣었다** — 위반 0건인 것과 검사기가 죽은 것은 화면에서
  똑같아 보인다. 136 의 실제 문장 5개를 `MUST_HIT` 에 박고 매 실행마다 먼저 돌려,
  어긋나면 **본 검사 전에 exit 2**. **그 자가 시험이 내 정규식 4개를 잡아냈다**(아래 함정).
- 🔴 **한국어를 부분 문자열로 맞추면 조용히 0건이 된다.** 「나뉘어진」 안에 「나뉘어지」가 **없다**.
  같은 날 agents 세션도 같은 병으로 목록이 **18편 → 63편** 달라졌다. 갈래가 둘이고 처방이
  다르다(어간 결합 = 어형 나열 · 띄어쓰기 변이 = `\s*`) → `docs/gotchas-data-collection.md` 맨 끝.
- **캐시 무효화**는 로컬·운영 둘 다 수행(`posts` + `post:<id>`).

**남은 것 · 주의**

- ⚠️ 🔴 **`AGENTS.md` 자동 로드 여유가 3B 다**(36,597 / 36,600). 내가 손대기 «전에» 이미
  36,596B 였다 — **한 줄도 못 넣는 상태였다.** 이번엔 유튜브 검사 2종이 22줄·65줄에
  **중복**돼 있어 그것만 걷어내 자리를 만들었다(규칙은 하나도 안 지웠다).
  다음에 한 줄이라도 넣으려면 **먼저 다이어트가 필요하다.**
- **전역 규칙 2개를 올렸다**(사용자 승인 2026-09-15) — `warn-claude-memory-mass-delete`(메모리
  대량 삭제는 이관이다 · 커밋 금지)와 `warn-korean-substring-match`(위 함정 · 발동 5 · 오탐 0).
  둘 다 훅에 직접 먹여 **양방향으로** 시험했다. 한쪽만 재면 「실패할 수 없는 검사기」가 산다.
- `~/.claude` 는 **삭제 208건을 커밋하지 않는다**(7일 경과분 클라우드 이관 · 워킹트리가 안전망).
  판별법은 「인덱스 끊긴 링크 0건」 → 전역 규칙 `warn-claude-memory-mass-delete` 로 올렸다.

**재개 지점**

- 보고서를 새로 쓰거나 고치면 **게시 전에** `python scripts/verify_translationese.py --run <원고>`.
- MarkLines 원문이 필요하면 **쿠키로 직접 받는다**(클리핑만 받으면 범례·색 데이터를 잃는다).
  받은 뒤 **`<table>` 개수를 먼저 센다** — 0개면 표 없는 보고서가 아니라 로그인이 안 된 것이다.
- **같은 원문으로 이미 올라간 글이 있는지 `source_url` 로 먼저 확인한다**(#79 가 그랬다).
  덮어쓸 때는 **기존 본문을 파일로 백업한 뒤에** UPDATE 한다.
- 🔴 **UI 확인용으로 무언가 띄웠으면 끝나고 반드시 닫는다.** 이번에 dev 서버(3007)와 `python.exe`
  콘솔 창을 띄워 두어 사용자 지적을 받았다. Playwright 는 `headless=True` 여도 **파이썬 자체가
  콘솔 서브시스템**이라 창이 뜬다 — `pythonw.exe` 나 `CREATE_NO_WINDOW` 로 숨길 것.
- `AGENTS.md` 에 무언가 추가할 일이 생기면 **다이어트가 먼저다**(여유 3B).
  이번 함정은 그래서 `AGENTS.md` 가 아니라 **`report.md` §3-6 과 `docs/oem-collection.md`** 에 적었다.

---

## 이전 상태 · 재개 지점 (2026-09-14) — 종목토론 수집을 되살리고 네이버 경로 다섯 갈래를 전수 확인했다

**무엇을 했나**

1. **종목토론 수집을 새 JSON API 로 옮겼다.** 직전 블록의 「남은 것」이다.
   - 🔴 **고칠 파일을 잘못 짚을 뻔했다.** 앞선 블록·문서가 `collect_naver_board.py` 를 지목했지만
     **아무도 그것을 부르지 않는다.** 실제로 도는 것은 GHA 가 `npx tsx` 로 돌리는
     **`scripts/collect_naver_board.ts` → `lib/naver/board.ts`** 다. 파이썬은 죽은 중복본이다.
   - 리서치와 달리 **호스트까지 바뀌었다** — 옛 URL 은 `stock.naver.com/domestic/stock/<코드>/discussion`
     으로 리다이렉트된다. HTML·JS 청크를 훑어도 안 나와서 **실브라우저 네트워크 로그로 잡았다.**
   - 새 주소: `stock.naver.com/api/community/discussion/posts/by-item`(목록) +
     `/posts/reactions`(조회수·공감). 로그인·쿠키 불필요.
2. **조용한 정지를 막는 exit 3 을 심었다.** 종목이 하나라도 실패하거나 **네 종목 모두
   `rawCount === 0`** 이면 실패로 끝낸다. 옛 수집기는 예외를 삼키고 exit 0 으로 끝나서
   GHA 의 3회 재시도조차 안 걸렸다 — 그것이 나흘 침묵의 진짜 원인이다.
3. **네이버를 쓰는 경로를 전수 확인했다**(2026-09-14 · 다섯 갈래) — 리서치 ✅복구됨 ·
   종목토론 ✅이번에 복구 · 상장사 뉴스(`m.stock.naver.com/api/news/stock/`) ✅그대로 ·
   한세 분봉(`api.finance.naver.com/siseJson`) ✅그대로 · 검색 보강 4종(`#main_pack`) ✅그대로.
   **개편은 리서치와 종목토론 두 갈래만 덮쳤다.**

**결정과 근거**

- 🔴 **목록의 `viewCount`·`recommendCount`·`notRecommendCount` 는 전부 0 이다.** 진짜 값은
  `reactions` 엔드포인트에만 있다. 목록 필드를 믿고 적재하면 **조회수·공감이 통째로 0** 이
  되는데 행 수는 정상이라 건수 점검으로는 안 걸린다. 화면 자신도 30개씩 묶어 따로 부른다.
- 🔴 **네이버 자동 글(`postType`이 `itemNews*` — 「5% 이상 하락했어요 😞」)을 수집 단계에서
  거른다**(사용자 결정). 최근 100건의 **15~30%** 이고, 투자자 심리가 아니라 주가를 기계적으로
  따라가는 글이라 감성 분석 입력으로 부적절하다. ⚠️ `excludesItemNews=true` 파라미터는
  **무시되므로**(실측 차이 0건) `postType === 'normal'` 로 직접 걸러야 한다.
  ⚠️ **옛 HTML 게시판은 함께 실었고 DB 에 109건 남아 있다**(2026-03~09 · 지우지 않았다) —
  그 구간만 범위가 다르니 감성 추이를 길게 견줄 때 감안할 것.
- **죽은 중복본 `scripts/collect_naver_board.py` 를 지웠다**(사용자 승인). 아무도 안 불렀고
  실행하면 조용히 0건을 냈다. 종목토론 수집은 이제 **타입스크립트 한 갈래뿐**이다.
- **본문은 계속 안 담는다.** 개편 뒤엔 본문이 목록 응답에 함께 와서 **공짜**지만(옛 구조는
  글당 1회 더 받아야 했다) 감성 분석이 제목만 쓰므로 종전 그대로 뒀다. 켜려면 인자 하나다.
- `writtenAt` 은 표준시가 안 붙어 온다(`2026-09-11T15:18:37` = KST). `+09:00` 을 붙여 읽는다 —
  GHA 는 UTC 라 안 붙이면 **9시간 밀린다.**

**막힌 곳 / 안 되더라**

- JS 청크 64개를 받아 API 경로를 캤지만 **0건**이었다. 웹팩 번들이 경로를 조각내 갖고 있어
  정규식으로는 안 잡힌다. **브라우저 네트워크 로그가 훨씬 빨랐다** — 다음엔 그것부터.
- `pageSize` 상한은 **100**(200 은 400 Bad Request). 페이지네이션은 쪽 번호가 아니라
  **커서**(`offset` = 직전 응답의 `lastOffset`)다.

**재개 지점**

1. **첫 GHA 실행 결과를 볼 것**(평일 09:00~15:30 30분마다). 로컬 실물 실행은 통과했다.
2. ⚠️ **과거 자동 글 109건을 지울지는 정하지 않았다.** 앞으로는 안 들어오지만 2026-03~09
   구간엔 남아 있어 **그 구간만 범위가 다르다.** 지우려면 `board_sentiment` 가
   `ON DELETE CASCADE` 라 감성 판정도 함께 사라진다는 점을 감안할 것.

**검증**: `npm run check-all` **EXIT=0**(476 passed — `lib/naver/board.test.ts` 17건 신설) ·
Python 상시 검사 5종 exit 0(`verify_docs`·`verify_news_relevance`·`verify_call_contracts`·
`verify_revalidate_tags`·`verify_report_sort_wiring`. 재무는 안 건드려 `verify_financials_sanity` 생략) ·
**실물 실행 exit 0** — 4종목 22건 처리(자동 글 2건 걸러짐)·신규 4건, 최신 글이 09-10 → **09-11** 로
갱신, 새로 들어간 자동 글 **0건**(기존 109건 그대로), 조회수 0인 행 **0건**,
KST 변환 대조(API `2026-09-11T15:18:37` → DB `06:18:37+00:00`) · 분봉 수집 실물 실행 658건(09-11).

---

## 이전 상태 · 재개 지점 (2026-09-11 밤 5) — 뉴스의 70%가 «남의 기사» 였다 · 네이버 개편으로 죽은 리서치 수집 복구

**무엇을 했나**

1. **뉴스 보통명사 오염을 끝냈다**(밤 3~4 블록의 「남은 것」). 원인이 **둘**이었다 —
   ①사명이 보통명사(용산·경신·대승) ②🔴 **질의에 따옴표가 없어** 구글이 사명을 토큰으로
   쪼갰다(「광진기계」→ 광진소방서 · 「한국캐스팅」→ 드라마 캐스팅). 두 번째는 이름 길이와
   무관해서 **긴 사명까지 오염**돼 있었다.
   - 실측: 한국 비상장 163곳 뉴스 **9,699건 중 6,787건(70%)** 이 오염. 163곳 중 **105곳이
     제목 일치율 0%**.
   - `collect_news.py` 를 고쳤다 — 두 글자는 `("(주)X" OR "㈜X") <맥락어>` **AND** + 제목 필터,
     세 글자 이상은 `"X" <맥락어>`. 맥락어는 로봇/자동차부품.
   - **36곳 6,787건을 지우고**(백업 있음) 고친 질의로 **260건**을 다시 받았다.
   - 검사기 `scripts/verify_news_relevance.py` 신설(**exit 0 이 정상**) + 단위 테스트
     `scripts/lib/test_news_query.py` 12건.
2. **「`/humanoid/research` 76/155편이 남았다」는 내 오보였다.** 실측하니 155 = 정리본 76 +
   **`excluded_at` 으로 의도적 제외 75** + **미처리 4**. 세 문서의 문구를 고쳤다.

**결정과 근거**

- 🔴 **두 글자 사명을 `OR` 로 이으면 안 된다.** `"(주)세정" OR "㈜세정" OR "세정 자동차부품"` 은
  20건 전부 **동명 타사**(세정그룹 의류)를 물어 왔다. `AND` 로 묶자 신영·용산·경신·호원·명신·
  동보·일진이 전부 진짜 회사 기사가 됐다. **A/B/C/D 네 벌을 실제로 던져 보고 골랐다.**
- 🔴 **제목 필터를 세 글자 이상에 걸면 진짜 뉴스가 죽는다.** 기사는 사명을 줄여 쓴다 —
  현대성우쏠라이트 → 「쏠라이트」(55/60 탈락) · 타타대우모빌리티 → 「타타대우」 ·
  HL클레무브 → 「HL 클레무브」(공백) · 한국지엠 → 「GM」. **처음에 전 구간에 걸었다가 되돌렸다.**
- **세정·삼송·나전이 0건이 되는 것을 그대로 뒀다** — 대시보드에는 **틀린 기사보다 빈칸이 낫다**.
- 검사기 임계는 **정리 후 실측**으로 정했다(비상장 부품사 최대 97건 → 상한 150).

**막힌 곳 / 안 되더라**

- ⚠️ **동명 타사는 못 가른다.** ㈜세정(자동차 부품)과 세정그룹(의류)은 이름만으로 가를 방법이
  없다. 여기까지가 한계이고 그 이상은 사람이 봐야 한다. 검사기 docstring 에 한계로 적었다.
- 삭제 스크립트가 처음에 **1,000건에서 잘렸다**(PostgREST 기본 한도). 백업이 불완전해질 뻔했다 —
  `.range()` 페이지네이션 + `.order()` 를 넣어 6,787건 전량을 받았고 **백업 건수 = 삭제 건수**로
  맞춘 뒤 실행했다.
- `AGENTS.md` 가 상한을 넘어(36,726/36,600) **검사기 줄을 못 넣을 뻔했다.** 상한을 올리지 않고
  「exit 0 이 정상」이 여섯 번 반복되던 것을 절 머리 한 줄로 묶어 자리를 만들었다(36,596).

**🔴 `~/.claude` 메모리 208개가 «사라진 것»으로 보이면 놀라지 말 것 — 이관이다**

워킹트리에서 메모리 파일 208개가 `D`(삭제)로 떠 있다. 내가 처음에 **「다른 세션의 미커밋
변경」으로 오판**했는데, 사용자가 바로잡았다 — **7일이 지난 메모리는 클라우드로 옮긴다.**
실측으로 기준이 지켜졌음을 확인했다(2026-09-11):

- 옮겨진 208개 중 **7일 안쪽인 것 0개** · `memory/` **밖에서 지워진 것 0개**
- `MEMORY.md` 인덱스의 **끊긴 링크 0개** · 09-04 이후 커밋에 등장했는데 옮겨진 것 **0개**
- 「오래됐는데 남은 6개」는 frontmatter `modified` 가 낡았을 뿐 **파일 시각은 09-04~09-10**
  이었다 — 판정은 파일 시각으로 제대로 됐다. **`modified` 값을 믿고 판단하지 말 것.**

⚠️ **옮긴 곳에 실제로 있는지는 확인 못 했다**(홈·OneDrive 를 뒤졌으나 없다 — 로컬 동기화
폴더가 아니다). 다만 208개 전부 `git` HEAD 에 남아 있어 `git checkout -- <경로>` 로 되살릴 수 있다.

**🔴 나흘 비어 있던 이유를 찾았다 — 스케줄이 아니라 네이버가 바뀌었다**

사용자가 「확인해봐」로 되물어 실제로 팠다. 로그(agents `logs/bot-20260911.log`)에
**09-11 15:00:49 「네이버 리포트 수집 종료코드=3」·「목록 파싱 0건 — 구조가 바뀌었을 수
있습니다」** 가 남아 있었다. **잡은 제시간에 돌았고 수집기가 정확히 경고했는데 아무도 안 봤다.**

- 원인: **네이버 금융이 Next.js 로 개편**됐다. 같은 URL 이 200 에 118KB 를 주는데
  `<table class="type_1">`·`nid=` 가 **0회**다. 차단이 아니라 진짜 구조 변경.
- 처방: **모바일 JSON API 로 갈아탔다**(`m.stock.naver.com/api/research/...`).
  수집·요약·재확인 3개 스크립트 + `lib/naver_research.py` + 테스트를 함께 옮겼다.
  **155 → 157건**으로 만회됐고 요약 경로(본문 345·441자)도 확인했다.
- 🔴 **`collect_naver_board.py`(종목토론)도 같이 죽어 있다** — `naver_board_posts` 최신
  2026-09-10 14:54. 사용자 결정으로 **이번엔 안 고쳤다**(「리서치만 먼저」).

**재개 지점**

1. ~~🔴 `collect_naver_board.py` 를 고칠지 정할 것~~ → **해결**(맨 위 블록). ⚠️ 다만 고칠 파일을
   여기서 잘못 짚었다 — 실제로 도는 것은 `.py` 가 아니라 **`collect_naver_board.ts`** 다.
2. ~~`/humanoid/research` 스케줄이 나흘째 비어 있다~~ → **해결**(위). — 마지막 수집·정리가 둘 다 2026-09-07 이고
   오늘이 09-11 이다(평일 15:00 `NAVER_RESEARCH_CRON`). 🔴 **「안 돌았다」로 단정하지 말 것** —
   새 리포트가 없었을 수도 있다. 로그가 **agents 레포**에 있어 이 레포에서는 확인이 안 된다.
3. **다음 뉴스 수집 뒤 `verify_news_relevance.py` 를 한 번 돌려 볼 것.** 고친 질의가 전체 432곳에
   대해 처음 도는 것이라, 이번에 손대지 않은 회사에서 새 위반이 나올 수 있다.
4. 남은 오차 하나: **대양전장**이 `"대양전장" 자동차부품` 으로도 대양전기공업 기사를 4건 물어 온다
   (구글의 느슨한 매칭). 건수가 작아 그대로 뒀다.

**검증**: `verify_news_relevance.py` exit 0(비상장 부품사 250곳 · 뉴스 1,858건 전수) ·
`verify_docs.py` 0건(AGENTS 36,596/36,600) · pytest `test_news_query.py` 12 passed ·
재수집 표본 12곳을 눈으로 확인(전부 회사·업계 기사) · 메모리 이관 208개 기준 검증(위반 0) ·
pytest 490 passed·3 skipped(naver_research 48건은 실물 JSON 픽스처로 새로 씀) ·
리서치 수집 실물 실행 exit 0(155 → 157건 · 캐시 무효화 200).

---

## 이전 상태 · 재개 지점 (2026-09-11 밤 3~4) — 재무 이상치 전수 대조 · 침묵하는 회귀를 잡는 검사기 2종 · ABB 백서 게시

**무엇을 했나**

1. **재무 전수 대조** — 553개사 2,890행에서 3배 급변 24건을 뽑아 **하나씩 DART 원문과
   대조**했다. 오류 9건을 고쳤고 남은 15건은 원문과 일치함을 확인했다.
   수집기 함정 4종을 고쳤다(전문 = `docs/gotchas-data-collection.md` 맨 아래 절):
   ①정정 공시 stub(화승 FY2021 별도 777억 → 연결 1.40조) ②단위 선언 우선순위
   (진영산업 1,000배 · 현대위아 100만분의 1) ③주석 번호 유입(현대트랜시스 영업이익 34)
   ④**재작성된 전기**(내가 멀쩡한 값을 덮었다가 되돌린 건).
2. **검사기 신설** `scripts/verify_financials_sanity.py`(exit 0 이 정상).
3. **보고서 게시** — ABB 로보틱스·엔비디아 공동 백서(피지컬 AI)를 `/humanoid/reports`
   에 올렸다(post id **136**, 본문 22,482자 · 도판 15장 · 원문 PDF 첨부).
4. **검사기 하나 더** `scripts/verify_call_contracts.py`(exit 0 이 정상) — 공용 헬퍼의
   반환형·시그니처가 어긋난 호출을 정적으로 잡는다. 이것이 **세 번째 피해자**
   (`collect_dart_labor.py`)를 찾아냈다. 눈으로 두 곳을 고치고 「다 고쳤다」고 보고한 뒤였다.
5. **에스케이온 FY2025 56.7조를 원문으로 확정** — 사업보고서 연혁에 합병 3건
   (2024-11 트레이딩인터내셔널 · 2025-02 엔텀 · 2025-11 엔무브)이 있고 「사업의 개요」가
   석유사업 2025년 매출을 49조 131억으로 적는다. 수집 오류가 아니다.

**결정과 근거**

- 🔴 **「DART 와 DB 가 다르다」는 「DB 가 틀렸다」가 아니다.** 중단사업 재분류로 전기가
  재작성되면 원보고서와 후속보고서가 **둘 다 맞다**. 그래서 고칠 해 N 을 받을 때
  **N+1 보고서도 함께** 받는다(`backfill_fnguide_gap.py` 의 `request_years`).
- 허용목록(`VERIFIED_JUMPS` 15건)은 「맞는 값」이 아니라 **「원문과 대조했다」**는 뜻이다.
  근거 한 줄 없이 추가하면 검사기가 무력해진다.
- `source` 빈 행 11건은 **기준선 방식**으로 뒀다 — 출처를 지어내는 것이 비워 두는 것보다
  나쁘다. 늘어날 때만 실패시킨다.

**막힌 곳 / 안 되더라**

- ABB 백서의 **공개 다운로드 URL 을 못 찾았다**(abb.com 페이지 타임아웃 · automate.org 403).
  `posts.source_url` 은 **null** 로 두고 문서번호(9AKK108472A9196)와 첨부 PDF 로 대신했다.
- `scripts/.env` 의 `NEXT_REVALIDATE_URL` 이 여전히 3000 을 가리켜 수집기의 자동
  무효화가 404 다. 프로덕션 엔드포인트로 직접 POST 해서 해결했다(200).

**현재 상태 (내 몫이 아닌 것)**

- `/humanoid/research` 증권사 리포트 — 수집 **155편** = 정리본 **76** + `excluded_at` 으로
  **의도적으로 제외한 75** + **미처리 4**(2026-09-11 실측).
  🔴 **「79편이 남았다」가 아니다.** 75편은 누락이 아니라 「정리하지 않는다」고 판정해 둔 것이고
  (기준은 증권사가 아니라 **요약 재료 유무** — 신한투자증권 2편은 네이버에 PDF 자체가 없다),
  화면 목록에서도 빠진다. 실제 남은 일은 **4편**이고 넷 다 PDF 가 있다.
  ⚠️ 종전 블록에 적힌 「76/155편 남음」은 **문서 문구를 검증 없이 옮겨 적은 것**이었다.
  agents 오케스트레이터가 평일 15:00 에 돌린다(`NAVER_RESEARCH_CRON`) — 이 레포 일이 아니다.
  다만 **마지막 수집·정리가 둘 다 2026-09-07 이라 나흘째 비어 있다**(로그가 이 레포 밖이라 원인 미확인).

**재개 지점**

밤 3 블록의 재개 지점 1·2(에스케이온 · 침묵하는 회귀)에 이어, **밤 4 에 적었던 셋도
둘이 닫혔다.**

- ✅ **`NEXT_REVALIDATE_URL`** — `scripts/.env` 를 프로덕션 주소로 고쳤다(실측 200).
  로컬 수집기도 «프로덕션 DB» 에 쓰므로 비울 캐시도 프로덕션이다. 종전값
  `localhost:3000` 은 두 가지로 틀렸다(3000 은 다른 앱 · dev 가 꺼져 있으면 받을 곳이 없다).
- ✅ **`consolidation` 라벨 훑기 — 안 하기로 했다.** 실측하니 **읽는 코드가 없다**
  (`lib/finance/types.ts` 는 다른 테이블이고, 챗봇 도구는 그 컬럼을 select 조차 안 한다).
  887행을 다시 받으려면 DART 호출이 몇 시간인데 화면에는 아무 변화가 없다.
  대신 **거짓 약속만 고쳤다** — 챗봇 시스템 프롬프트가 「consolidation=consolidated 우선」
  이라고 적는데 도구는 그 컬럼을 안 준다. 「연결/별도를 단정하지 말 것」으로 바꿨다.

- ✅ **「신원」 레코드 삭제** (사용자 지시). active 「신원」(ticker 자리 = 「신원자동차」)은
  `dart_corp_code` **01404896 이 공시 0건**인데 재무가 5년치 있었고, `business_summary`·
  `products` 는 **의류 상장사 신원(009270)** 것이었다. CASCADE 로 재무 5 · 페이지 1 ·
  **뉴스 1,076행**이 함께 지워졌다. 남은 「신원」은 hidden 인 의류 상장사 하나뿐이다.

**남은 것 — 🔴 회사명이 «보통명사» 인 회사들의 뉴스가 통째로 오염돼 있다** ✅ **밤 5 에 처리했다**(↑ 맨 위 블록)

「신원」을 지우기 전에 뉴스를 열어 보니 **1,076건 전량이 무관한 기사**였다(「신원 확인」·
「신원 미상」). 표본으로 확인한 다른 회사도 같다 — **용산**(1,789건, 용산구·용산공원) ·
**경신**(1,569건, 「지지율 경신」) · **대승**(709건, 축구 「대승」). 상세 = `gotchas-data-collection.md` 맨 아래 절.

- **규모**: 두 글자 한글 회사명 39곳이 뉴스 **8,621건** · 100건 초과 13곳이 **8,242건**.
  전체 뉴스 26,883건의 **31%** 다.
- ⚠️ **이름 길이로 일괄 삭제하면 안 된다** — 「기아」(1,454건)는 진짜 회사 기사다.
  회사당 표본을 열어 보고 가려야 한다.
- **처방 방향** → ✅ **밤 5 에 했다**(맨 위 블록). 실제 처방은 여기 적은 것과 달랐다 —
  맥락어만으로는 모자랐고 **질의에 따옴표를 씌우는 것**이 더 큰 몫이었다.
- 🔴 **왜 여태 안 보였나** — 뉴스는 개수만 늘 뿐 「틀린 값」이 아니라 어떤 검사에도 안 걸린다.
  **검사기가 없는 데이터는 아무도 안 본다.**

**검증**: pytest 483 passed · 3 skipped · verify_docs 0건 · verify_financials_sanity exit 0
· verify_call_contracts exit 0(파일 195 · 튜플 반환 76) · verify_revalidate_tags 통과
· dev(3001)에서 보고서 실물 확인(raw `**` 0 · raw `<br>` 0 · 본문 이미지 15장 전부 로드
· 콘솔 오류 0) · 프로덕션 캐시 무효화 200.

---

## 이전 상태 · 재개 지점 (2026-09-11 밤 2) — 감사 잔여 3건 완주 · fnguide 가 버린 과거 연도를 DART 로 복구

### 무엇을 했나 (커밋 7건 · 전부 푸시됨)

| 커밋                | 내용                                                                  |
| ------------------- | --------------------------------------------------------------------- |
| `20e9f23`           | `/oem` 중국 판매량이 **내수+수출**임을 실측하고 화면에 주석           |
| `6f90fd3`·`6627ba6` | ROADMAP Phase 3.5·5·6 정리 → **미완 체크박스 0건**                    |
| `abdd0e3`           | 휴머노이드 보고서 **정렬이 죽어 있던 것** 수리 + 여백을 게시판과 일치 |
| `c2b0f9b`           | DART 연도매핑 버그 + 제품군 카테고리 배선                             |
| `ab13f0d`           | 제품군 재분류 **2,033건** 반영                                        |
| `a0acde2`           | hidden 회사 유령 재무 차단 + `/humanoid/research` 여백                |
| `aa559c4`           | fnguide 가 버린 FY2021~22 를 DART 로 복구 + **연결 우선**             |

### 결정과 근거

- **제품군 경계 규칙은 사용자가 정했다** — 조명·와이퍼 = `전장`(안전은 에어백·ADAS 처럼
  사고 대응만) · 차량용 반도체는 용도별 분산하되 **`기타`로 내리지 않는다**.
  정본은 `scripts/lib/product_categories.py` 의 `_BOUNDARY_RULES` 이고 **수집기도 같이 쓴다**.
- **모델은 Sonnet** — Haiku 는 경계에서 흔들려 「릴레이 → 구동계」처럼 **맞던 값을 망가뜨렸다.**
  전수 검토 비용이 모델 차액보다 비싸다.
- **일진복합소재는 지우지 않고 hidden + merged_into 로 둔다**(이 레포는 상태로 관리한다).

### 막힌 곳 / 안 되더라 (이번에 값비싸게 배운 것)

- 🔴 **LLM 재분류의 `--apply` 가 LLM 을 다시 부르면 안 된다** — 비결정적이라 사용자가 검토한
  것과 다른 값이 들어간다. 저장된 diff 를 그대로 반영하게 고쳤다.
- 🔴 **후보를 안 가르면 맞던 값까지 망가진다** — 자동차/로봇 22종을 통째로 주면 자동차 부품이
  로봇 칸으로 샌다. 예시를 붙여도 안 막히고 **후보에서 빼야** 막힌다.
- 🔴 **DART 보강이 처음에 «별도재무제표» 를 넣었다**(원인 3 + 함정 3, 전문 =
  `docs/gotchas-data-collection.md`). 요지: 「감사보고서제출」은 감사보고서가 아니다 ·
  본문 노드를 길이로 고르면 별도를 집는다 · 빈 껍데기 연결(len=128)을 집으면 통째 실패 ·
  `consolidation` 을 보고서명으로 정하면 안 된다 · `_dedup_rows` 를 반드시 쓴다.
- 🔴 **잘못 채운 값은 「그럴듯해서」 검사에 안 걸린다** — 그래서 `--redo-dart` 를 뒀다.

### 재개 지점

1. 🔴 **화승코퍼레이션 FY2021 이 아직 별도 777억**이다(실제 연결 약 1.4조). 원인은 찾았다 —
   FY2021 **사업보고서에 연결재무제표가 없고** 연결 값은 **FY2022 보고서의 「전기」** 에 있다.
   처방: `backfill_fnguide_gap.py` 가 깨진 연도 N 을 요청할 때 **N+1 보고서도 함께** 받으면
   `_dedup_rows` 가 최신 보고서를 우선해 연결이 이긴다(3줄 · 재실행 20분).
2. **별개 계통의 기존 오류 5건** — 진영산업 FY2023 이 1,354배(단위 오류) · 현대위아·현대트랜시스
   (`pykrx+dart` 등 다른 경로) · 케이피엑스헌츠만 · 디엔오토모티브. 이번 범위 밖이다.
3. `/humanoid/research` 는 여백만 맞췄다. 증권사 리포트는 정리본 **76편**이 화면에 오른다
   (수집 155 = 정리 76 + 의도적 제외 75 + 미처리 4. 2026-09-11 실측으로 정정).

### 검증

`npm run check-all` EXIT=0 · pytest **451 passed** · `verify_docs` 0건 ·
`verify_report_sort_wiring` exit 0(양방향 시험) · 프로덕션 캐시 무효화 200 ·
dev·프로덕션 화면 실물 확인. 영업이익 > 매출 **3건 → 0건** · `consolidated` **0 → 198행**.

---

## 이전 상태 · 재개 지점 (2026-09-11 밤) — 캡션 판정을 3단으로 올리고 C: 여유 1.3GB 를 21GB 회수했다

### 무엇을 했나

**① 게시된 리포트 132 의 캡션 2건을 고쳤다**(사용자 지적 — 「고치는 비용이 작은 것만」)

- 화면 「약 23조 5,000억 원」을 캡션이 「약 23조 원」으로 줄여 놨다 → **3곳** 정정
  (🔴 내가 처음 「2곳」이라 보고했는데 DB 를 세어 보니 3곳이었다 — 맨 뒤 요약 목록을 빠뜨렸다)
- 화면 「재쿠 7」 ↔ 본문·캡션 「제쿠 7」 표기 갈림 → 본문 21곳을 안 건드리고 **캡션에만 병기**
- `posts` 132 UPDATE 후 `post:132` 태그 무효화(HTTP 200). 본문 29,236 → 29,271자(+35, 계산치와 일치)

**② 규칙을 두 번 고쳤다** — 두 번째는 사용자 지적으로 **원인 진단을 정정**한 것이다

- 1차(`1ea86c5`): §2-B-1 수치 반올림·표기 갈림 · §2-B-2 자막에만 있고 화면엔 없는 장면
- 🔴 2차(`98f53b2`): 1차에 _"기계로는 못 잡는다 → 사람 눈 감사에서만"_ 이라 썼는데 **틀렸다.**
  사용자 지적 _"캡쳐한 사진은 너가 직접 읽어서 확인할 수 있지 않아?"_ 가 맞다 — `Read` 가 PNG 를 그대로 띄운다.
  **샌 원인은 못 봐서가 아니라 판정이 「있나」 1단이어서**였다(감사 `note` 에 두 건 다 적혀 있는데 `pass: true`).
  → §2-B 판정을 **①있나 ②자릿수까지 같나 ③표기가 같나** 3단으로 올리고 §3-3 절차에 명시

**③ C: 디스크가 여유 1.33GB(0.6%)까지 차 있었다** → 약 21.4GB 회수

| 한 일                       | 회수        | 누가           |
| --------------------------- | ----------- | -------------- |
| `powercfg /h off`           | **12.64GB** | 사용자 직접    |
| `uv cache clean`            | **8.30GB**  | 나 (36만 파일) |
| `scripts/_yt_report/**.mp4` | 0.44GB      | 나 (71개)      |
| `Temp` 7일 초과분           | 0.0002GB    | 나 (48개)      |

### 🔴 재개 지점

- **투란 프로젝트(`workspace/1.테스트/turan`)가 재빌드 불가에 가깝다** — 원본 39개(6.1GB) 전부 OneDrive
  클라우드 전용 · **`.mlt` 타임라인 0개** · 나레이션·그래픽 소실. 실측과 푸는 순서를 그 레포 `CLAUDE.md`
  「재빌드 가능 상태 점검」 절에 적어 뒀다. **v11 얘기가 나오면 거기부터 읽을 것**
- **캡컷은 이 PC 에 없다**(설치본·무설치본·압축본 전부 0건, `~/.claude` 9,170파일 스캔 포함).
  사용자가 기억하는 것은 **Shotcut portable**(`C:\Tools\Shotcut\` · melt 7.41.0)로 보인다
- 디스크는 또 찬다 — 남은 카드는 **OneDrive 「공간 확보」**(수십 GB · 사용자 직접) ·
  `pnpm`+`npm-cache` 4.1GB(**dev 서버 3개 내린 뒤에**) · DISM 2~5GB
- 🔴 `claudevm.bundle` 9.63GB 는 **지우지 말 것** — 로컬 에이전트 세션 542개, 마지막 사용 09-10

### 막힌 곳 / 안 되더라

- **`Grep` 도구가 `.jsonl` 트랜스크립트를 통째로 건너뛴다** — `Shotcut` 은 찾는데 같은 트리의 대화 기록은
  0건이 나왔다. 파이썬으로 바이트를 직접 읽어야 했다(`gotchas-ci-deploy.md` §8 과 같은 뿌리)
- **투란 세션 트랜스크립트 4개가 이미 삭제돼 있다**(`~/.claude/projects` 에 turan 폴더 자체가 없다).
  사용자 입력은 `history.jsonl` 에 남아 우회로 복원했다 — **입력 원문은 트랜스크립트와 별도 파일이다**
- OneDrive 폴더를 재귀로 읽는 스캔은 **클라우드 전용 파일을 내려받게 만들 수 있다** → 중단하고 범위에서 뺐다

### 검증

`npm run check-all` **EXIT=0**(lint 0 errors·2 pre-existing warnings / format OK / typecheck OK / **test 459/459**) ·
`verify_docs.py` EXIT=0 · `verify_yt_report.py` EXIT=0 · prettier check OK.

⚠️ **워크트리에 다른 세션의 미커밋 변경이 있다**(`components/humanoid/research-list.tsx` ·
`docs/gotchas-data-collection.md` · `scripts/collect_dart_audit.py` · `scripts/dart_eval/*`).
**커밋에서 제외했다.**

---

## 이전 상태 · 재개 지점 (2026-09-11 저녁) — `/oem` 중국 판매량이 「내수 + 수출」임을 실측하고 못 박았다

### 무엇을 했나

사용자 질문(_「oem 국가별 판매량의 중국 34백만대가 내수와 수출을 모두 합한 수치인가 —
다른 기관은 2025년 중국 내수를 25백만대라 한다」_)에서 출발한 데이터 정의 확인이다.
코드 로직은 건드리지 않았고, **사실을 문서 3곳과 화면 2곳에 남겼다.**

### 결정과 근거 — 「판매의 country = 판매 시장」이 중국에서는 거짓이다

기존 문서(`docs/oem-collection.md` · `Architecture.md`)는 판매의 `country` 를 일관되게
**판매 시장**이라 적어 왔는데, **중국 브랜드는 그렇지 않다.** 세 방향으로 확인했다.

| 대조              | DB            | 외부 실측                                                | 판정            |
| ----------------- | ------------- | -------------------------------------------------------- | --------------- |
| 2025 China 합계   | 34,352,181    | CAAM 총 판매 34.40백만(수출 7.098백만 포함)              | 일치            |
| 2024 China 합계   | 31,428,710    | CAAM 총 판매 31.44백만                                   | 일치            |
| 2025 China 의 BYD | **4,602,436** | BYD **글로벌** 총 판매 **4,602,400**(해외 104.96만 별도) | 사실상 동일     |
| 2025 전 국가 합   | 98,473,177    | 글로벌 신차 91.9백만(GlobalData)·91.69백만(S&P)          | 약 6.6백만 과대 |

🔴 **BYD 대조가 결정적이다** — 중국 내수는 약 355만대인데 China 칸에 글로벌 460만대가 통째로
들어 있고, **그와 별개로** 해외 국가 행에 612,416대가 또 잡힌다(러시아의 Chery 20.6만 ·
GWM 19.8만도 같다). 즉 **국가별 값의 단순 합계는 시장 규모가 아니다.**

사용자가 본 「25백만대」는 기준이 다른 값이었다 — CPCA 승용차 **소매** 23.744백만
(또는 승용차 내수 도매 약 25~26백만). CAAM 국내 판매(상용 포함)는 **27.302백만**이다.

### 막힌 곳 / 안 되더라

- **MarkLines 원본 엑셀에는 정의 메타가 없다**(1행이 비어 있고 2행이 바로 헤더). 정의를 원문에서
  확인할 길이 없어 **외부 공식 통계와의 대조**로 판정했다 — 같은 질문이 또 오면 이 방법이 정답이다.
- **Turbopack 캐시가 깨져 `/login` 이 응답하지 않았다**(`aggregation_update.rs` 패닉 다발).
  `docs/gotchas-playwright-ui.md` 처방대로 `.next` 삭제 후 재기동하니 5.6초에 떴다. 코드 회귀 아님.
- Playwright 첫 진입은 컴파일 때문에 30초를 넘긴다 → `curl` 로 `/login`·`/oem` 을 먼저 데운 뒤
  `wait_until="domcontentloaded"` 로 들어가면 안정적이다.

### 재개 지점

이 건은 **완결됐다.** 이어서 볼 만한 것은 둘이다(둘 다 이번에 건드리지 않았다).

1. **`/oem` 국가별 표를 「내수 기준」으로 보여 줄지는 미결**이다. 지금은 캡션으로 경고만 한다.
   내수 기준으로 바꾸려면 CAAM 수출 통계를 별도 수집해 차감해야 하는데 수집기가 없다 —
   **할지 말지가 사용자 결정 사항**이고, 하기로 하면 그것이 새 수집기 한 벌이다.
2. 🔴 **ROADMAP 에 남은 일은 없다**(같은 날 저녁 정리). 이 자리에 처음 적었던
   「Phase 5 미완 3건(E2E 확대 · Lighthouse · 배포 체크)」은 **틀린 보고였다** — 옛 블록의
   문구를 실물 확인 없이 옮겨 적은 것이다. 실제로는 ① **Phase 5 가 아니라 Phase 6** 이고
   ② **E2E 는 2026-09-10 에 이미 완료**됐으며(`e2e_smoke.py` 65칸 + `e2e_interact.py`
   아홉 화면 64건, 스크립트가 레포에 있다) ③ 남아 있던 둘은 **사용자 판정으로 닫혔다** —
   성능(Lighthouse)은 **목표 철회**, 프로덕션 배포는 **잴 것이 없어 종료**. Phase 6 = ✅ 완료.
   🔴 **같은 날 Phase 3.5·5 도 닫았다** — 둘 다 「헤더만 진행 중」이었다.
   - **Phase 5**(대시보드 UI)는 미완 체크박스가 **0개**였다.
   - **Phase 3.5**(fnguide)는 사용자 지적(_「fnguide 작업 이미 했을텐데 확인해봐」_)대로 이미 끝나 있었다.
     실측: `verify_fnguide.py` **36건 실패 0** · 주간 GHA 연속 성공(최근 09-07) ·
     `source='fnguide'` **1,850행 · 185개사 · FY2026** · valley.town 은 코드에서 소멸
     (`requirements-phase3.txt` 첫 줄 주석만 남음). 🔴 **`collect-financials.yml` 의 마지막 실행이
     2026-07-14 인 것은 고장이 아니라 분기 스케줄(1/4/7/10월 14일)이다** — 다음은 10/14.
   - 그래서 **Phase 0~6 전부 ✅ 완료**다. 남은 미완 3건은 fnguide 가 아니라 **2026-07-18 감사 잔여**로,
     Phase 3.5 에 얹혀 있을 뿐이다(세진 연도매핑 · 제품군 name-level 오분류 · 기존 products map 미적용).
     셋 다 LOW 이고 현 데이터에 해가 없다. 특히 마지막 건은 **재수집 없이는 안 줄어든다**
     (2026-09-11 실측 — `'기타'` 1,860건으로 여전히 최다).

### 검증

`npm run check-all` EXIT=0(prettier ✓ · typecheck ✓ · 27파일 459건 ✓ · lint 경고 2건은
이번 변경과 무관한 기존 파일) · `verify_docs.py` 오류 0 · 경고 0 ·
**dev 서버 1440px·390px 두 폭에서 `/oem` 캡션 실물 확인**(스크린샷). dev 서버는 검증 후 종료했다.

---

## 이전 상태 · 재개 지점 (2026-09-11) — 로봇 글 휴머노이드 일원화 · 유튜브 5편 · 캡처 품질 규칙 신설

**무엇을 했나**

- **로봇 글을 `/humanoid` 전용으로** — 카테고리 SSOT `lib/reports/robot.ts` 신설, `PostRepository.list({excludeCategory})`,
  `/humanoid/reports/[id]` 신설(본문은 `components/reports/post-detail.tsx` 공용 — 복제 안 함).
  `PostList`·`PostPagination` 에 `basePath` 를 달아 정렬·페이지 링크가 게시판으로 튀던 버그도 같이 고쳤다.
- **증권사 리포트를 보고서 표 UI로 평면화** — `research-list.tsx` 재작성(리포트 1건 = 1행).
  `groupReports` 는 쓰는 데가 없어져 지우고, 그 자리 테스트를 `listTargets`·`mapFigures` 로 채웠다.
- **유튜브 5편 게시(131~135)** — 자동차 2 · 로봇 3. 본문 76,517자 · 프레임 51장.
- **캡처 품질 규칙·검사기 신설** — `docs/report-from-youtube.md` · `docs/report-from-pdf.md` ·
  `scripts/yt_report/settle.py` · `scripts/verify_yt_report.py` · 훅 `block-yt-publish-without-checks`.
- **리포트 115·117·122 수리** — 각각 `<br>` 평문 노출 · 체리/BYD/지리 미완성 막대 · 폭스바겐/도요타 판매량 빈칸.

**결정과 근거** (계획서 = `docs/plans/2026-09-11-robot-consolidation-and-yt-reports.md`)

| #   | 결정                                             | 근거                                                                                                                                            |
| --- | ------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | 증권사 리포트 = **완전 평면화**                  | 사용자 선택. 묶음·펼치기 폐기                                                                                                                   |
| 2   | 오퍼스는 **수동 작성 경로에만**                  | 사용자 원문 _"자동으로 할 때는 지금의 방식 그대로 하고, 내가 클로드코드에서 작성할 때 오퍼스를 쓰라는 이야기야"_ — 자동 경로 모델 상수는 그대로 |
| 4   | 로봇 분류 안전망 = **제목 키워드 강제**          | 분류기가 목록 밖 값(`글로벌 자동차`·`해외사업`)을 실제로 만들어 왔다                                                                            |
| 5   | 필터·페이지 넘김은 **클라이언트 유지**           | URL 방식은 증권사 17 × 종목 50 × 기간 × 페이지 조합마다 캐시가 쌓여 ISR Write 재발                                                              |
| 6   | 로봇 글 상세 = **`/humanoid/reports/[id]` 신설** | 사용자 선택                                                                                                                                     |

**막힌 곳 / 안 되더라**

- 🔴 **도해가 «차오르는 도중» 캡처되는 것이 이 워크플로의 최빈 결함**이다. 사람 눈으로 「표가 보인다」로
  통과시키기 때문에 vision 에이전트도 놓친다. `settle.py` 가 51장 중 6장을 잡았고 그중 하나는
  영업이익률이 `6.0o` 로 **타이핑 중**이었다. 자막 띠(하단 24%)를 빼지 않으면 오탐이 절반이다.
- 🔴 **캡션은 자막에서 들은 수치를 적고 그림에는 없는 경우가 45%(23/51)** 였다. 「대충 맞나」로 보면 안 잡힌다 —
  캡션의 고유명사·수치를 **낱낱이 뽑아 하나씩 O/X** 해야 잡힌다.
- 🔴 **v3(딥엑스)는 72분 내내 자료 화면이 0장**이었다(217프레임 전수 확인). 토킹헤드 70% · 협찬 광고 10%.
  억지로 채우지 않고 썸네일만 썼다. **협찬 ETF 배너를 자료로 쓰면 안 된다.**
- `montage.py` 에 `-y` 가 없어 **재몽타주가 옛 이미지를 남기면서 `OK` 를 찍고 있었다**(수리).
- `settle.py --only` 가 기록을 덮어써 51건이 8건으로 줄었다 — 내가 만든 `verify_yt_report.py` 가 잡았다(수리).
- 전수 스캔 시트의 **칸 → 시각 환산은 `(N-1)×20 + 10`초**다. `(N-1)×20` 으로 안내해 조사자 둘이 10초 이른 화면을 짚었다.

**재개 지점**

1. `/humanoid/research` 는 **정리본 76건만** 화면에 오른다(수집 155건).
   🔴 **「나머지 79건이 남았다」는 틀렸다**(2026-09-11 실측) — 75건은 `excluded_at` 으로 의도적 제외이고
   실제 미처리는 4건이다. 그 4건도 agents 오케스트레이터 몫이다.
2. 유튜브 회차를 새로 돌리면 **`settle.py --check` → `verify_yt_report.py` → 캡션 대조** 순서를 지킬 것.
   훅이 `publish.ts` 를 막으므로 검사를 건너뛸 수 없다.
3. 남은 캡션 손질 후보(합격이되 개선 여지): `v2_honda_loss` 「23조」→「23조 5,000억」 · `v2_jaecoo_uk_rank` 「재쿠/제쿠」 표기.
4. `v1_business_segments`·`v1_related_party_96`·`v2_suzuki_japan_rank` 3장은 **영상에 해당 화면이 없어 드롭**했다.
   재시도하려면 타임코드를 처음부터 다시 찾아야 한다(±25초 구간은 전부 다른 내용).

---
