# 재생성 가능한 중간 데이터 (API 재호출 8분)
major_2y.json
*.zip
_tmp.*
# Q1a 층1 스캔 캐시 — 목록 653KB · 건별 파싱 1.3MB.
# 🔴 raw 는 지우지 말 것: `--reparse` 가 이 파일로 **API 호출 0회** 재계산을 한다.
#    (정규식을 고칠 때마다 2,181건을 다시 받으면 8분씩 든다)
scan_all_list.json
scan_all_raw.jsonl
_scan_log.txt
fetch_opinion_result.json
q2_targets.json
_fetch_log.txt
_pair_log.txt
fetch_opinion_last_run.json

# 진단·탐침 산출물 — 재생성 가능하므로 추적하지 않는다 (AGENTS.md 「진단 산출물은 임시」).
# 계약·샘플로 남겨야 할 것이 생기면 파일명을 !예외로 적고 prettier 포맷을 맞출 것.
*.json
